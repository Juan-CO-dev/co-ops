/**
 * Form field primitives — Phase 5.
 *
 * Pattern from prototype: defaultValue + onBlur (no controlled re-render
 * per keystroke, mobile-friendly), label above input, dark theme.
 *
 * Components: NumberField, TextField, TextArea, SelectField, ToggleField, Row.
 */
export {};
