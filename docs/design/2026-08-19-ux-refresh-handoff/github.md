repo: Juan-CO-dev/co-ops
branch: main

## Last sync
date: 2026-08-19T21:24:00Z 2026-08-17T05:58:40Z

### Updated in this project
- 13 controls given align-items:center. My chip pass had bundled centering inside an "if display is absent" branch, so any control that already declared a display got the taller 44px box and never got centering — 9a's "Edit · 2h left" sat with a 16px top/bottom imbalance. Source pairs the two in all 422 occurrences (ActionButton BASE is "inline-flex min-h-[44px] items-center ..."), so a 44px control without items-center does not exist upstream. Unbundled: centring is now set on any single-line flex control at the floor regardless of what else it declares
- 302 padding-sized chips given min-height:44px, plus 8 declared-under-44 controls and 3 checkbox label rows. My previous predicate returned early when NO height was declared, so every control sized purely by padding fell out — 153 of them — which is exactly the "missing declaration = skip" exemption this file already records a rule against. The population is the source's rounded-full border-2 px-3 text-xs font-bold filter/nav chip, which carries min-h-[44px] in all five places it appears (CatalogClient:172, SkuCatalogClient:307, MenuClient:244, ToastTab:115, SalesTab:101), so these were unfaithful as well as short — 22a's roster row had a 5.6px spread between siblings of one class
- Checkbox rows now carry the 44px hit area the source puts on the LABEL (FulfillmentClient:215, PricingForm:98) while the box itself stays 20px
- ICON BUTTONS: 41 controls that declare BOTH width and height squared to 44x44. My previous 44px pass raised only the height axis, so 5 circular buttons rendered as ELLIPSES (36x44 at border-radius 999px) and 7 as distorted rounded squares; it also gated on "has a border", which split stepper pairs (a bordered "-" at 36x44 beside its borderless "+" at 28x28). Rule: an element declaring both axes is an icon button — raise both together or neither, and never let a border condition divide one control group. 3 empty checkbox visuals restored to 20px, since the source keeps a small box inside a 44px LABEL hit area (FulfillmentClient.tsx:215) rather than enlarging the box itself. The 4 remaining non-square controls are 27a's deliberately wide 56x48 tap-ticks
- 121 controls raised to the repo's 44px minimum (from 28/32/34/36/38/40/42px). The convention is min-h-[44px] with 422 matches across components/, and MenuClient.tsx:125 is 20a's "Sizes (N)" toggle verbatim — my recreations had drifted below it, which only became visible once the box-sizing inflation stopped padding them out. Applied by predicate (bordered co-tap, declared 28-43px) rather than to the flagged instances, so 16a's Add pickers and every other drifted control closed in the same edit; sub-28px indicators like 29a's tick were left alone
- 355 more bordered controls given box-sizing:border-box, with NO size condition. My earlier sweeps carried an arbitrary ">= 40px" floor that has no basis in source — the app runs under Tailwind's border-box reset, which applies to every element regardless of size — so 72 controls under 40px stayed content-box and painted 3.2px over their declared height, visibly shortening 6 control rows (20a's "Sizes (2)" disclosure toggles beside 44px chips, 16a's two Add pickers, 29a's tick). Zero bordered content-box controls remain; the 195 borderless co-taps are correctly unaffected
- STANDING RULE, now with the two ways I kept breaking it: apply a source-derived fix by predicate across the whole document IN THE SAME EDIT, and put NO arbitrary threshold or section list in the predicate. Both an invented floor (">= 40px") and a missing declaration ("no height, so skip") silently exempt exactly the population the fix was for
- Phone tap-target floor applied BY PREDICATE across all 30 width:390px frames: 25 more targets raised to 44px, zero remain under the floor. My previous pass had walked only the two sections a report happened to name — the exact failure the standing rule in this file was written to prevent, one turn after writing it
- The missed pair (2a "Verify") declared NEITHER height nor min-height, so a predicate requiring a declared size >= 40px skipped them entirely; their 32px came from 11px content plus padding plus a border on content-box. RULE ADDENDUM: treat a MISSING declaration as 0, never as "skip" — an absent property is a value, not an exemption
- 56 more bordered controls given box-sizing:border-box — my earlier predicate keyed on "min-height:" only, so every control declaring "height:" was skipped and painted 3.2px over its declared size (27a's receiving tap-ticks declared 48x56 and rendered 51.2x59.2, overhanging their 44px row siblings). Zero bordered content-box controls remain. SIXTH instance of keying on one spelling of a value — the predicate must cover every spelling the property has
- 15 phone-frame tap targets raised to the project's 44px floor (3a response chips at 32.4px, 9a location badges at 35.2px) — the mobile-pass note claiming phone controls were authored fresh at 44px was not true for chips and badges carrying .co-tap
- COMPLETED TWO TIER-1-ONLY FIXES DOCUMENT-WIDE (the "we don't want it incomplete" class): 436 controls given box-sizing:border-box, and 260 card shells brought to .co-card's authoritative 16px radius. Zero controls remain content-box; zero card shells off 16px
- Both were the SAME defect I had already fixed twice inside tier-1 and never propagated. content-box puts a 2px border OUTSIDE min-height, so a bordered and an unbordered control in one flex row land ~3.2px apart — that was causing 5 visibly mismatched button pairs (18a "Save as default"/"Print 7 sheets", 18a "Select all"/"Reprint 3 sheets", 15a "Search"/"Add lead", 8a "Photo"/"Send", 27a "Replace photo"/"IMG"). Card radii were spread across 7 values with several sections MIXING internally (20a had 14px x5 + 12px x8, 18a 16px x3 + 18px x7, 23a 16px x6 + 12px x5)
- STANDING RULE for the build and for any future pass here: when a fix is derived from source, apply it by PREDICATE across the whole document in the same edit. Every one of this session's repeat findings came from scoping a correct fix to the sections I happened to be looking at
- CONTRAST CONVERGED: 88 AA failures -> 0 of 6876 text nodes. Last 7 fixed by ELEMENT, not by value: 6a "Act now" and 18a's three dim labels had backgrounds INHERITED from an ancestor, so the same-attribute predicate skipped them (fixed by band containment — walk the #FFE560 band's span and retint inside it); 27a "IMG" 4.43 -> 4.9 via #6B5211; 8a "!" light-on-#9E8C4A 3.16 -> ink 5.53
- 8a "Must read" was flipped THREE times across rounds because its composited ground is #371A1B (a .15 red tint over the #141414 band), not the flat #141414 each round assumed — so both red tokens sit near threshold there. Settled with cream text at 13.4:1, which is token-independent. Lesson: when a fix oscillates, the ground assumption is wrong, not the colour choice
- PROBE CORRECTNESS (cost several false reports): composite alpha FRONT-TO-BACK, acc += a*(1-acc), starting at the element's OWN background; forcing alpha to 1 after the first translucent layer stops the walk before the dark band underneath and falsely reports light-on-dark text as invisible
- COMPOSITED-CONTRAST PASS: a per-value grep kept reporting the colours clean while 88 AA failures persisted, because contrast depends on the EFFECTIVE COMPOSITED background, not the declared one. Resolved each failing text node by walking ancestors and compositing alpha, then applied by role: 36 cta labels on their own 10% tint (2.83-3.09:1) to #B3252C; the ONE dark-band case ("Must read" on #371A1B) to --co-cta; 22 dim labels on warm/neutral tints (3.78-4.43:1) to --co-text-muted; 38 info-blue text uses to --role-moo #1F4E79; 4 light-on-solid-cta labels to ink (4.56:1); 3 ink-on-info to white (4.77:1)
- REGRESSION I INTRODUCED AND FIXED IN ONE ROUND: the #9E8C4A -> #7A6015 mapping picked a value chosen for a TEXT role, but 34 of those instances were role-badge BACKGROUNDS carrying ink text — #141414 on #7A6015 is 3.08:1 vs 5.53:1 on #9E8C4A. Backgrounds reverted; text uses keep #7A6015. Same "correct value, wrong element class" failure as the border tokens
- UPSTREAM TOKEN ISSUE for the spec (not a divergence to hide): the source's own destructive spelling bg-co-cta + text-co-surface (ItemQuestions.tsx:259, RecipeBuilder.tsx:494) only reaches 4.06:1, so matching source still misses AA on a solid red fill. This document uses ink on cta (4.56:1) instead — the app needs either a darker cta or ink-on-red as its destructive pairing
- KNOWN FALSE POSITIVES in any ancestor-walking contrast probe here: an effBg walker that reads only backgroundColor resolves a GRADIENT band to transparent and falls through to the page ground, which reported 23 cream-on-cream and 14 gold-on-cream phantoms. Read backgroundImage too, or treat same-colour-on-itself as a probe artefact
- COLOUR RECONCILIATION COMPLETE — every rendered colour now audits against app/globals.css. 23 distinct colours: 16 token-backed, 7 documented deliberate, ZERO unreconciled. Prior notes claiming reconciliation were premature; this is the audited state
- The 7 deliberate non-token values, each with its reason: #FFFDF5 second surface level for nested/inset panels; #B3252C AA cta text on LIGHT grounds (the source's own bg-co-cta/15 + text-co-cta pairing is 2.91:1); #7A6015 AA gold text (--co-gold-deep is ~1.4:1 as text); #C9A227 gold border/fill ONLY, never text (non-text needs 3:1); #1F7A44 + #14532D confirm affordance (ActionButton has no green variant); #B47C00 MVP star; #FFF3D4 body-gradient stop
- Fixed this pass: white-on-#C9A227 on the three irreversible-confirm buttons (PO confirm, credit reconcile, receiving duplicate) was 2.42:1 — the least readable text in the document on its most consequential actions — now ink-on-gold at 7.62:1, matching ActionButton primary rather than inventing a light-on-gold pairing. 37 cta labels on dark bands moved to --co-cta #FF3A44 (5.21:1) since #B3252C is a light-ground token. Collapsed 9 non-token near-duplicates onto the tokens whose role they duplicated: #FBFAF4/#F1EDE0 -> --co-surface, #F6F0DC -> --co-bg (lifting 14 dim labels from 4.47 to 4.84), #8A6D1F -> #7A6015 (4.46 -> 5.44 on --co-surface-2), #E4F3E9 -> --co-success-surface (globals.css notes that tint was itself consolidated from two values), #EFE6C9 -> --co-card-border, #0EA5E9 -> --co-info, #C41E28 -> #B3252C, #E5E5E5 -> --co-text-faint, #9E8C4A -> #7A6015, #1F5FA0 -> --co-info, #9CCC9C -> --co-success, #256B41 -> #14532D
- TWO METHOD TRAPS worth carrying into the build: (1) an audit regex of /#[0-9A-Fa-f]{6}/ matches HTML NUMERIC ENTITIES — &#128205; &#127775; read as "colours" and produced 8 phantom findings; strip &#\d+; first. (2) my band predicate "background contains rgba(255,58,68" matched the light red TINT as well as dark bands, reverting 19 AA pill labels to 2.91:1 in the same edit that fixed 37 real ones — test for the DARK band explicitly, never for "any coloured background"
- Type floor: zero text nodes under 9px document-wide (6809 checked)
- GROUND SWAP COMPLETED ACROSS ALL SPELLINGS: the hex pass fixed 225 #F3EEDC but the same colour was also written rgba(243,238,220,a) in 284 places, and my post-edit grep tested only the hex so it reported a false clean. Two creams ended up side by side on the same dark masthead (solid centre title vs translucent crumb/role labels on 36a-39a). All spellings now resolve to --co-bg #FFF9E4; zero of any spelling remain. LESSON: verify a colour change by counting EVERY spelling (hex, rgb, rgba) — same failure class as the earlier dashed-vs-solid and inherited-uppercase misses
- CORRECTION TO EARLIER SPEC GUIDANCE: the note calling #C9A227 an "accessible dark gold" replacement for --co-gold-deep was WRONG — as text it measures 2.38:1, worse than the 4.5:1 floor and no real improvement on the token it replaced. All 11 text uses moved to #7A6015, which measures 5.98:1 on white and 5.67:1 on --co-bg. #C9A227 remains on 168 borders and fills, where it is legitimate (non-text contrast only needs 3:1). Recommendation for the build: add an AA-passing dark-gold TEXT token (#7A6015) and keep --co-gold-deep #E6C84A for pressed states
- 18a label-sheet thumbnail: 33 text nodes were authored at 5.5-6.5px with no scaled ancestor — genuinely illegible, not a scaling artefact. The sheet preview exists to show ARRANGEMENT (labels per sheet) and the frame already shows one disc at actual size, so the thumbnail now renders type as rules with a caption saying so, rather than faking text at 5.5px
- GROUND-TOKEN CORRECTION, and it invalidates earlier contrast numbers: every app frame in this document was painted #F3EEDC, a value that appears NOWHERE in app/globals.css. The real page ground is --co-bg #FFF9E4 (Mayo), with the body gradient #FFFDF5 -> #FFF3D4 over it. Swapped all 225 instances to #FFF9E4. Unusually for this document a VALUE-keyed replace was correct rather than role-keyed, because #F3EEDC played exactly two roles and both resolve to the same token: the frame/page ground (--co-bg) and light-on-dark masthead text (text-co-bg, per StorefrontOrderTray.tsx:390 / StorefrontPackages.tsx:82) — the on-dark role reads 17.48:1 after the swap
- CONTRAST NOW MEASURED AGAINST THE REAL GROUND: --co-text-dim #6E6E6E 4.84:1 (was 4.39 and FAILING AA on the invented ground), --co-text-muted #4A4A4A 8.41:1, #8A6D1F gold head 4.65:1 (was 4.22, failing), ink 17.48:1, #B3252C cta 6.19:1. 179 labels that read sub-AA were an artefact of the wrong ground, not real app defects — any accessibility finding in this file taken before this fix understated the real app by ~0.45 of a ratio point and must be re-derived from these numbers
- The one genuine contrast finding SURVIVES the correction: the source's tinted cta pill (bg-co-cta/15 + text-co-cta) still measures 2.91:1 because that pairing is computed against the pill's own tint, not the page ground — the #B3252C substitution remains the right call at 5.35:1
- Group-head colour axis applied: 185 heads at #4A4A4A vs 374 field labels at #6E6E6E, so the roles differ on all three source axes (size / tracking / colour). 9 #8A6D1F accents kept (22a numbered step heads, 34a count heads). 5 on-dark labels restored to their band treatment — my group-header script tested geometry but never colour, so a #FFE560 label on a #141414 band got the light-surface spec, where muted/dim tokens have no meaning. Exclude by ANCESTOR BAND, not by colour literal
- LABEL SWEEP COMPLETE — the two label roles now hold across all 39 sections: 198 group heads at 12px/700/0.025em (tracking-wide) and 416 field/sub labels at 10-12px/700/0.12em, 614 labels total. Group heads classified by live-DOM structure (label followed by a container with >=2 element children), then applied to the file via entity-normalized text matching (strip &entities; and non-alphanumerics before comparing) because the file stores &mdash; / &#39; / emoji that DOM textContent does not
- 55 DELIBERATE exceptions kept, all in three places: 14a's customer portal keeps its own looser storefront eyebrows (0.16/0.18/0.28em) and 0.14em form labels, matching app/order/* which really is a distinct grammar (rounded-3xl, border-co-border/70); 23a's login wordmark "OPERATIONS" at 0.32em; 16a's "Step 1 of 2" counter at 0.28em
- Probe gotcha worth recording: the DOM normalizes inline styles to "prop: value" WITH a space, so a regex keyed on "text-transform:uppercase" against getAttribute('style') silently matches nothing — an early sweep pass reported 0 labels in 32 sections for exactly this reason. Use getComputedStyle for classification and the no-space form only against the FILE
- PASS 3 CORRECTION: my first attempt collapsed BOTH label roles onto 0.12em because I quoted "tracking-wide" from the source without converting it — Tailwind tracking-wide is 0.025em, NOT 0.12em, and 0.12em is the FIELD-label value. Restored the two roles: 60 group headers (labels heading a row group, classified by live-DOM structure: label followed by a container with >=2 element children) now read 12px/700/0.025em, field and sub labels stay 10-11px/700/0.12em. Verified in-file: 1a:10, 3a:8, 4a:13, 6a:20, 22a:9 group headers
- 6a "Refresh" at 12px/0.08em is CORRECT and was wrongly flagged as a straggler — it inherits tracking from its parent control, and 0.08em is a real source value for small control labels (LtoClient, SkuPackChainPanel:157, ChecklistItem:1875 all use text-xs font-bold uppercase tracking-[0.08em])
- FAILURE CLASS, now 4 for 4 this session: every regression was a correct value applied to the wrong ELEMENT CLASS (control vs panel, colour vs geometry, content-box vs border-box, group header vs field label). The token names encode the class — co-border for panels, co-border-2 for control emphasis, co-card-border for the card hairline, tracking-wide for group heads, tracking-[0.12em] for fields — so any replacement keyed on the old VALUE rather than the element ROLE will over-reach. Key on role
- TOOLING NOTE for future verification: this document is ~1.3MB single-file, and html-to-image screenshots time out on it. Visual regressions here can only be caught by DOM probing. Also: eval_js runs against the LAST RENDERED page, so probe results are stale until a re-render — confirm against the file when they disagree
 Tier-1 carried TEN spellings of one label class (11/700/0.16em/#6E6E6E dominant at 46 instances, plus 0.18em, 0.2em, 0.14em, 0.12em, 0.1em, 0.08em at 10/11/12px). 0.16em — my most-used value — appears NOWHERE in the source
- Source label grammar, read from components/reports-hub/* and components/prep/*: reports-hub section label h2 = "mb-1 px-1 text-xs font-bold uppercase tracking-wide text-co-text-muted" (12px/700/0.025em/#4A4A4A, identical in Cash/Checklist/Opening/Pm detail views); prep sub-label = "text-[10px] sm:text-[11px] font-bold uppercase tracking-[0.12em] text-co-text-dim"; ReportFilterBar field labels + MiscSection/MixedPrepSection toggles + AmPrepForm = text-xs font-bold uppercase tracking-[0.12em]; 0.14em is RESERVED for section headers (PrepSection.tsx:86 text-lg/0.14em — its own docstring states it — and UnifiedSearchResults group h2 at text-xs/0.14em/text-co-gold-deep)
- Normalized 127 tier-1 labels (10-12px, dim/muted/ink/cta colours) to 0.12em / weight 700. Excluded by predicate: pills and badges (border-radius), the pass-1 controls (min-height>=44), 13px+ section headers, and dark-band frame chrome (#F3EEDC / #FFE560 mastheads are design-frame styling, not app UI)
- REMAINING for a later sweep if wanted: the same label drift exists outside tier-1 (sections 5a and 7a-39a were not touched this pass)
- POLISH PASS 2 — read the three canonical primitives I had never opened: app/globals.css (the token file), components/ui/PageHeader.tsx, components/ui/SummaryRow.tsx
- .co-card is a GLOBAL CLASS, authoritative for every card: background var(--co-surface) / border 1px solid var(--co-card-border) #F0E7C4 / border-radius 1rem = 16px / box-shadow var(--co-shadow-card). This doc's soft warm shadow "0 1px 2px rgba(0,0,0,.04), 0 8px 24px rgba(120,100,20,.09)" is EXACTLY --co-shadow-card, and the phone-shell gradient is exactly the real body gradient — both already faithful. Normalized 20 tier-1 card shells from 14/18/22px to 16px
- Fixed an error I introduced in pass 1: ActionButton secondary is bg-co-surface, and --co-surface is #FFFFFF, not #FFFDF5. 10 secondaries corrected
- TOKEN DISCREPANCIES for the spec (NOT swept — both are deliberate and defensible, but the build must decide): (1) --co-gold-deep is #E6C84A (pressed Mustard); this doc uses #C9A227 in 179 places, chosen earlier BECAUSE the real gold-deep on Mayo measures ~1.4:1 and was unreadable as a label colour — recommend keeping an accessible dark-gold token and reserving #E6C84A for pressed states only. (2) --co-surface is #FFFFFF; this doc uses #FFFDF5 as a nested/inset fill in 900 places, which reads as a deliberate two-level surface system rather than drift
- PageHeader locks the page-title tuple (h1 text-xl/font-extrabold/leading-tight, p mt-1/text-sm/text-co-text-muted) — this doc's frame titles run 26-30px at weight 900, a deliberate presentation choice for 1280px design frames, not app chrome. Flagged rather than changed
- POLISH PASS 1 — traffic-ranked. Tier 1 = per-shift/every-employee: login (23a), dashboard (1a/4a), the four shift reports (1a/6a), reports hub (3a), cash (22a), chrome (34a). Audit found NO breakage: zero contrast failures under 3:1, and 6a's 26 sub-44 targets are all on DESKTOP frames (mouse) with its phone frame clean
- Read components/ActionButton.tsx — THE canonical CO-OPS action control, and its own docstring names the exact drift my recreations had reproduced: "replaces the drifted per-surface copies (rounded-md vs rounded-xl, min-h 48 vs 52, ad-hoc disabled treatments) that accumulated across Build #1-C.43". Canonical: rounded-xl(12px) + border-2 ALWAYS + uppercase + tracking-[0.1em] + font-bold; default min-h-48/px-5/text-sm, lg min-h-56/px-6/text-base; primary = border-co-TEXT(#141414)+bg-co-gold, secondary = border-co-border-2(#F0E7C4)+bg-co-surface+text-co-text, danger = red OUTLINE on surface (brand book: red is never a fill for actions)
- Normalized 16 primary + 10 secondary CTAs across 1a/3a/4a/6a/22a to that spec. Deliberately NOT touched: rounded-full filter chips, sub-44 small controls, 12px pill labels, the green confirm affordance and dashed add-slot (no ActionButton variant), and 33a's 8px/44px/gold-deep buttons — those are the ADMIN FORM grammar, which the source really does use
- THREE distinct button grammars confirmed in source, all legitimate: (1) ActionButton 12px/48-56px/border-ink — operational primaries, (2) admin forms 8px rounded-lg/44px/border-gold-deep, (3) catering+portal ink-fill w/ cta text, rounded-xl, tracking-0.12em. Do not collapse them
- Swept 7 solid-red count pills to the source's real treatment: bg-co-cta/15 tint + red text, rounded-full, px-2 py-0.5 (10 instances in source). Used #B3252C rather than #FF3A44 for the text — the source pairing measures 2.91:1, the darker red gives 5.35:1. FLAG FOR SPEC: worth a token fix upstream, since every tinted cta pill in the app has this contrast
- Group 4 repairs: (1) 21a phone said "4 matches" against FIVE desktop rows and asserted "Last pull failed — 401 from Toast", a state 21a never establishes (its desktop baseline is the opposite: the nightly pull ALREADY RUNS; the failing pull with its error string belongs to 10a's admin hub). Now "5 matches to review", banner reframed to what the queue actually contains (two name matches, three modifiers), list captioned as a partial like 38a's, and the third rule rewritten around modifier DISPOSITION rather than a pull failure. (2) 17a phone dropped the invented "Effective Aug 1, 2026" — no effective-date field exists on the desktop rule. (3) 12a Dan Kim's role column reads "Inactive", matching the desktop (EMP is Luis Torres)
- RECURRING FAILURE, now four times: writing a phone frame from MEMORY of a neighbouring section instead of from the desktop frame it sits beside. For any remaining paired frame, read the desktop's own strings and counts first
- MOBILE GROUP 4 (admin — laptop is the honest primary): 390px frames added to 17a (pricing rule, READ-ONLY on phone with an "open on a computer to edit" footer, because changing a rate reprices every open quote; missing-rule warning still travels; 0.00% gratuity still prints in dim tone), 21a (Toast crosswalk — ONLY the match queue comes along, confirm/reject at 44px side by side, pull failure leads because a 401 makes every suggestion stale; GUID fields + JSON event log deliberately not offered) and 12a (people roster reads at 44px, search replaces filter chips, the four-column desktop table is not attempted; role-change step-up + session-revocation warning travel because the consequence travels)
- Phone controls authored FRESH at 44px per the group-3 lesson — no desktop chip helpers copied. All three verified sub-44-free; rules cards 376px border-box
- MOBILE PASS COMPLETE. Remaining: polish pass on high-traffic surfaces, then the build spec
- 15a stage pills raised 38px -> 44px. ROOT CAUSE: I copied a DESKTOP chip helper into a phone frame, so the pills carried a desktop 38px minimum while every other phone tap in the pass is 44-51px. The pills are the phone board's primary navigation, so a miss loses the board. Row still fits: 108+104+62 + 2×7 gaps = 288 inside 390−28 = 362
- Audited every co-tap inside all eight phone shells afterwards: no other sub-44 target. 26a's four unsized taps are full-width multi-line delivery rows; 30a's small chips are in its DESKTOP frames
- SPEC NOTE: phone frames must not reuse desktop chip/button helpers — the minimum differs (44px vs 38px). Group 4 to author phone controls fresh rather than copying
- 28a phone PO total corrected AGAIN, this time derived from the four lines the frame itself prints: 2×$42.80 + 1×$8.90 + 4×$18.00 + 2×$48.60 = $263.70. My $297.00 was CAP-0809-SYS's total (4/2/6 cases, no Capicola) — a three-days-older order. Label reverted to "Estimated": the frame is a DRAFT with a live cutoff and a Place order button, so "Ordered total" (the reconcile frame's term for a placed order) was wrong for the state
- LESSON for the spec and for me: three wrong totals in a row on one small frame, each borrowed from a neighbouring record. Money on a summary line must be COMPUTED from the lines rendered beside it, never lifted from another frame that looks similar
- Mobile group 3 repairs: (1) 16a phone quote re-based on the desktop's CURRENT v2 column — tax $94.82, total $1,042.98, deposit $260.75, balance $782.23 (852+28+68.16+94.82 checks out); my figures were the pre-revision tax. (2) 28a phone PO rebuilt from the desktop draft's own four lines — Capicola, whole was missing — and "Estimated $486.20" replaced with "Ordered total $297.00"; $486.20 was 27a's INVOICE total for the delivery against that PO, a billed figure reused as an order estimate. (3) The "What changes at 390" cards lacked box-sizing:border-box (NOT inherited from the section) so they computed 439px and ate the 56px right gutter — now 376px border-box, fitting 390+24+376 = 790 inside the 797 content box
- SPEC NOTE: box-sizing is not inherited. Any fixed-width card carrying padding + border needs box-sizing:border-box explicitly, or its used width exceeds its authored width
- MOBILE GROUP 3 (manager surfaces): 390px frames added to 15a (pipeline — six stage COLUMNS become a scrolling pill row carrying per-stage counts, follow-ups promoted to the first card, Add lead pinned; drag-to-move degrades to the Move-to list already inside each lead), 16a (quote detail — money first, five line items collapsed behind a tap, Accept/Decline share a 44px row, Resend keeps its password badge, Revise + Print labels move to an overflow as non-floor actions) and 28a (PO — numeric table becomes stepper cards, cutoff clock pinned top, Place order gold over a quiet Save draft)
- Each phone frame ships beside a "What changes at 390" card naming the three decisions, so the rebuild has the reasoning and not just the layout
- REMAINING: mobile group 4 (admin — laptop is the honest primary, frames need to prove nothing breaks), then the polish pass, then the build spec
- 39a read-only state corrected after re-reading ReportReferenceItem: readOnly kills the LINK, not the Edit affordance. The component renders EITHER an <a> wrapper (interactive) OR a plain <div> (read-only) and the Edit button is a sibling gated only on canEdit && onEdit — so a finalized closing still offers Edit to a key holder. Also: read-only PENDING rows are the ones that drop to opacity-70; read-only COMPLETED rows keep full opacity because their receipt still stands
- Added 39a: ReportReferenceItem — the LAST component in the repo without a frame. Four closing rows the closer cannot tick: two auto-completed (GREEN tick, not the mustard every other completed row uses, so the closer knows it came from a report not a tap; one with the KH-only Edit text-link, one without), one pending-and-tappable to its source page, one pending-and-read-only at 70% opacity because closing is finalised. Verbatim closing.report_ref.* strings
- FINDINGS: (1) the green tick is colour doing real semantic work, and the stated reason this could not be a branch inside ChecklistItem; (2) the unrecognised-report-type fallback to AM Prep is scar tissue — the route was once hardcoded and a first-shift smoke test caught the misroute on the opening row; (3) attribution collapses a 2+ submitter chain via a shared formatter and switches ARIA strings to match rather than reading truncated visible text
- COVERAGE NOW CLOSED: 83/83 routes and every substantial component drawn from source across 39 turns. Index + 38a's card updated to say so. Remaining: mobile pass groups 3-4 (manager, admin), polish pass on the daily surfaces (incl. 3a vs 34a's two ReportList row shapes), then the Claude Code build spec (~50 findings)
- Added an INDEX section at the top of the document: 38 anchored rows (newest first), each naming a turn and what it covers, with "+ phone" markers on the five sections that carry a 390px frame (14a, 24a, 26a, 27a, 30a). Plus two standing cards — what is still to come, and how to read a frame (recreation by default; departures always marked Proposed / my addition / changed with a note on what source does instead)
- MOBILE PASS: customer group done. 14a's review-and-reserve step now pairs a 390px phone beside the locked desktop frame — stack order follows reading order (price, coverage, deposit), the deposit figure repeats in the pinned bar because the total has scrolled away by then, and all labels/figures match the locked frame verbatim ($391.00 subtotal, "Tip for the crew (10%)", "Estimated DC tax", 25% deposit = $126.12)
- REMAINING: mobile pass groups 3-4 (manager surfaces, admin), a polish pass on the daily-use screens (incl. reconciling 3a's and 34a's two different ReportList row shapes), then the Claude Code build spec
- MOBILE PASS, door-and-floor group: 390px phone frames added beside the desktop frames in 26a (receiving list — credits panel moves below and stays collapsed, badges wrap to their own line, primary action pins bottom), 24a (par pass — one SKU per card instead of a table row, stepper + field as a single full-width 44px band, empty stays an em dash so unobserved and "we're full" remain distinct) and 30a (inventory audit — on-hand reference becomes a tappable row that opens on its own, since the laptop's side-by-side drift math has no room)
- MOBILE RULE, learned the hard way in 27a: CHECK EACH ROW'S FRAME FLEX BEFORE INSERTING. 27a was the one section authored flex:1/max-width:620px, so a fixed 390px phone frame crushed both desktop frames to 179px. All frames are now flex:none;width:620px and rows scroll (27a = 1678px)
- MOBILE PASS STARTED (Juan's call: phone frames BESIDE the desktop ones, grouped by surface family). First group = the door: 27a now carries a 390px phone frame beside its two desktop forms. Rules established for the rest of the pass — two columns collapse to one, secondary fields hide behind the PO banner, the file bar pins to the viewport not the page, and every tap target stays 44px (the one measurement that cannot shrink)
- MOBILE PASS QUEUE (all drawn at 1280 only so far): 14a customer funnel, 15a/16a catering CRM, 17a-20a admin, 21a crosswalk, 22a-24a cash/maintenance/settings + auth + par walk, 25a ordering, 26a receiving list, 28a PO panel, 29a stubs, 30a-38a the rest
- 9a directory tiles: names no longer clip. ROOT CAUSE was the trailing badge, not the avatar — in a 171px 2-up tile (10px padding, 30px avatar, 8px gaps) a "Lead"/"You" chip on the NAME's row left it ~71px while "Juan Rivera" needs 74 and "Marco Reyes" 81. Moved both chips down beside the role label, which is short (OWN / KH) and has room. ProfileDirectory.tsx read for the real tile metrics (h/w 30px avatar, gap-2, p-2.5, truncate on the name) — the source does truncate by design, so the fix is to stop competing for the row rather than to drop the ellipsis
- 38a temps reconciled with locked 3a: signals.tempFlags is ONE field feeding both ReportList's TEMP badge and ChecklistReportDetail's signal clause, and 3a's Aug 11 closing row carries no TEMP badge — so tempFlags is 0. Dropped the "1 temp out of range" clause, brought the reach-in reading to 39, un-flagged its row, and rewrote the note so it no longer asserts an out-of-range reading. 3a's Opening Aug 12 row keeps the 44 degree walk-in
- 38a reconciled with locked 3a: the Aug 11 closing is AUTO-FINALIZED and never submitted, not Confirmed. Drawing it that way surfaced a real bug — ChecklistReportDetail's STATUS_LABEL_KEYS covers only open/in_progress/submitted/confirmed and falls back to the RAW column value, so an auto-finalized closing renders "auto_finalized" and an incomplete confirmation renders "incomplete_confirmed" on screen, even though reports.status.auto_finalized ("Auto-finalized") and reports.status.incomplete_confirmed ("Confirmed (incomplete)") both exist and the reports LIST already prints them properly. Two missing map entries, on the report a manager opens after an unfinished night. Item list also captioned as a partial (2 of 6 stations, 6 of 20 lines)
- Added 38a: ChecklistReportDetail (one view for opening/closing/am_prep/mid_day) — header + status pill, completion/par/temp signal line, Counts & readings block ABOVE the stations, station groups with tick/dash, by-name, per-line answers, L5-gated notes and ungated photo links
- FINDINGS: (1) ONE column holds three answer kinds — a yes_no line stores 0/1 in the same count_value a temperature uses and a free_text line stores its answer in the NOTE field; interpretAnswer untangles all three and the readings block must exclude question lines explicitly or it prints "Bathroom stocked? 1"; (2) a free-text answer is L5-gated only because it lives in the note field — correct outcome reached indirectly, and a rebuild that separates them would quietly expose it; (3) skipped and undone render the same em dash even though the signal line counts them separately; (4) photo links are ungated while the note beside them is nulled below L5
- STILL OWED: ReportReferenceItem only — then mobile, polish, spec
- CLAIM RETRACTED + replaced with the real one, after reading app/api/admin/checklist-templates/[id]/items/route.ts and lib/admin/templates.ts addPrepItem: a line created by AddPrepItemForm is NOT shapeless. The server derives columns as shapeToColumns(sectionDef.shape, input.includeNote) — every new line inherits its SECTION's shape. The real limitation is that you cannot create a line whose shape differs from its section, so a MIXED section (33a's Catering station = count + yes/no + free text) is unbuildable from this form; each line must be created and then converted via the separate per-line /items/[itemId]/input-type route. 37a's proposed picker is now justified on that basis and pre-selects the section's shape
- 37a: added a PROPOSED input-type picker to AddPrepItemForm (Count / Portioned / Line count / Yes-No / Text) at Juan's request. FINDING behind it: the form's POST body carries NO input type — a line's shape is inferred later from prep_meta.columns via shapeFromColumns, which this form never writes, so a line created here has no shape until someone opens the prep editor. The five labels already exist as admin.templates.prep.input_type.* and are already user-visible as the overview panel's chips, so this is a one-field addition rather than a new concept
- 37a Capitol Hill block rebuilt from locked 33a: Catering station = Herb aioli (Count) + "Catering trays labelled and dated?" (Yes/No) + "Anything the PM shift should know?" (Text); Misc = all four toggles. Sunday Gravy REMOVED as a prep line — it is the master-list item that "Cambro of gravy" links to, not a line. Advisory arithmetic preserved (badge 5, es-fill 29/31) by moving the no-Spanish chip to a restored line
- Added 37a: AddPrepItemForm (section select, EN/ES labels, optional par + unit defaulting to an em dash, "Include a note field" ONLY when section === Misc, "Also create an Opening verification item" ONLY on am_prep and defaulting ON, Tier B step-up) and PrepOverviewPanel (default-collapsed, zero edit affordances, advisory badge = needsLink + esMissing + inputTypeDrift, per location×subtype block w/ es-fill + needs-link counts, per-line input-type chip + needs-link + no-Spanish chips, cross-location input-type drift block, Open Prep Editor deep links)
- Advisory counts reconciled: badge "5 to review" = 2 needs-link + 2 no-Spanish + 1 drift; es-fill 29/31 against the 2 no-Spanish chips
- FINDINGS: (1) adding one AM Prep line creates TWO by default — the opening mirror checkbox is pre-ticked and has no help text; (2) the note field is section-conditional (Misc only), which is why 33a's Cook bacon row has a note and its siblings don't; (3) the overview panel deliberately has no edit affordances and its findings stay OUT of the builder's Doctor totals, because prep is never publish-versioned with closing/opening — one editor per thing, enforced by omission; (4) input-type drift across locations is surfaced but unenforced, so two shops' prep sheets can be quietly incomparable — a product question for the spec
- STILL OWED: ChecklistReportDetail, ReportReferenceItem — then mobile, polish, spec
- 36a cross-frame reconciliation. CASH: tips $86.00, note verbatim "Two comped orders rang after the drawer pull", on-shift = Marco + Ana only (Luis was an UNTICKED option in 22a's picker) — same stored record as 22a. PM: Marco's Aug 12 gradients set to 9a's Great/Great/Good/Great (9a's aggregate is Great 5 / Good 3 / Needs work 0, so any needs-work for Marco is impossible, not merely inconsistent), tally recomputed from the two cards to 2/0/0, 2/0/0, 1/1/0, 2/0/0, and the submitter changed to Pete Dolan so the manager isn't naming herself MVP
- METHOD NOTE for the spec: when two frames render the same stored row, the LOCKED frame is authoritative and every derived figure (tallies, sums, rosters) must be recomputed, not adjusted
- Added 36a: CashReportDetail (six money rows w/ visible arithmetic — drawer − float = deposit, deposit vs projected = over; over/short in green, short in red, exact reads "even"; over-short note null below L5) and PmReportDetail (MVP + note, gradient tally, per-employee eval cards w/ four dimensions, area-to-improve public + manager-only note, shift activity w/ per-person items/reports and per-report progress). Cash figures reconcile with 22a (1898.85 − 200 = 1698.85; − 1641.00 projected = +57.85). Tally sums to 2 per dimension because two evals are drawn — it is DERIVED from that list
- FINDINGS: (1) redaction lives in the LOADER, never the view — a field the viewer can't read arrives null, so there is no conditional hiding a value that was still sent; house rule for anything role-scoped; (2) one eval record, two audiences — Marco's area-to-improve is the same sentence 9a shows him, with a manager-only note beneath he never sees; (3) the tally is computed only from evals the viewer could already open, so it can't become a side channel; (4) shift activity omits "overdue" deliberately — on a historical report that would be a claim about today
- STILL OWED: ChecklistReportDetail (largest of the hub views), ReportReferenceItem, PrepOverviewPanel, AddPrepItemForm — then mobile, polish, spec
- 35a corrections: the step-up masthead no longer says "Level 5 and up" — PasswordModal's comment means level-5+ is the action's TARGET, not the actor's eligibility; the actor gate is role-based (step_up_not_available -> "Step-up not available for your role"), consistent with hasEmailAuth and Juan's PIN-primary-below-manager correction. Also dropped an invented call-site count (a repo-wide grep for requestStepUp("A")|<PasswordModal> is BOUNDED at 400 of 1156 files, so no total is defensible — 46 requestStepUp sites + 6 direct mounts within the scanned subset), and fixed three prose counts (nine sections mention step-up, ten mention either modal, three hub detail views remain)
- SCAN #5 (all 173 .tsx enumerated + classified). Found EIGHT more surfaces referenced only in my own prose, never drawn. Added 35a for the two biggest: PasswordModal (step-up: title/subtitle verbatim, wrong-password inline, defensive lockout banner, Escape cancels, dead session closes to the route's own enforcement) and RevokeReasonModal (bottom-sheet on phone / centred on desktop, radio re-enter_count|other, note REQUIRED when other so confirm renders disabled, backdrop-tap cancels)
- FINDINGS: (1) a wrong step-up password deliberately does NOT lock — actor is already authenticated, so failures go to the audit log instead; the lockout banner is defensive for an account locked from the login screen; (2) Escape/backdrop rules differ per modal ON PURPOSE — step-up and revoke cancel because backing out is safe, the idle warning ignores Escape because only an explicit choice is safe; (3) the silent-vs-structured revoke boundary is the SERVER's to decide — the client attempts the plain undo and reacts to the refusal, because a 60s clock-skew edge would be wrong in both directions. House rule for any time-boxed permission
- STILL OWED (read-and-draw, no open questions): components/reports-hub/{CashReportDetail,ChecklistReportDetail,PmReportDetail}.tsx, components/ReportReferenceItem.tsx, components/admin/template-builder/PrepOverviewPanel.tsx, components/admin/templates/AddPrepItemForm.tsx — then the mobile pass, polish, and the build spec
- 34a repairs: blurb counter now derives from the text (49/500), Save renders its disabled treatment beside "Saved" (the success branch sets savedBlurb === blurb, so the button MUST be disabled at that moment), small cta text uses the darker #B3252C per the doc's token note, the unified-search page chip that lacked the query substring became "User Management", and the reports rows were rebuilt on components/reports-hub/ReportList.tsx — date + type pill, "By: {name}", "Status: {label}", signal badges (under-par in cta, temp flag in gold), and the per-row search snippet with its field label
- Added 34a — the last four surfaces: UserMenu (initial-only trigger, Signed-in-as, EN/ES pessimistic language toggle, AGM+ profile-blurb editor w/ 500-char counter, Dashboard link that self-hides on /dashboard, Sign out), ManagerLoginForm (per-field error placement, 423 lockout banner w/ live countdown, constant-shape reset reply, 8-char client-side minimum), IdleTimeoutWarning (9:30 trigger, 30s countdown, undismissable — Esc ignored, inert backdrop, 2-button tab trap, listeners detached while open), UnifiedSearchResults (People / Pages above the reports, page chips carrying the location through chipHref)
- FINDINGS: (1) UserMenu grew from a language toggle into the app's only global chrome after operations pages were found to be dead ends — which is why an identity menu owns the profile blurb; (2) login error PLACEMENT is the design: wrong-password + inactive under Password, unverified under Email, lockout as a banner, reset reply identical either way so the form can't enumerate staff; (3) the idle modal mirrors a server-owned timeout, so a drifted mirror can show an already-expired countdown; (4) reports.search.placeholder "Search name or type" undersells a box that also returns people and pages — a one-string fix
- COVERAGE COMPLETE: 83/83 routes and every substantial component in components/** now has a pass. Remaining work = the MOBILE PASS (everything from 14a on is drawn at 1280 only), then polish on the highest-traffic surfaces, then the Claude Code build spec
- 33a repairs after reading components/prep/PrepSection.tsx + the am_prep.error.* block: (1) canLink is a LIST-WIDE prop so all three needs-link rows show the picker — one row without one was an impossible state; (2) the Misc validation error is am_prep.error.primary_required = "Required" (I had invented "Answer yes or no before submitting."); (3) PrepSection's header carries NO item count — it is an inline-block uppercase h3 with a co-gold-deep bottom border, so "4 items"/"3 items" was invented chrome; (4) one note field left empty so am_prep.misc.notes_placeholder actually renders
- SPEC NOTE: the AM Prep error vocabulary is terse by design — "Required", "Enter a number", "Cannot be negative" per field, with "Fix {count} issues before submitting" as the only sentence, in the summary
- Added 33a: NeedsLinkQueue (count lines pointing at neither item nor SKU; All/Items/SKUs filter chips, search-capped-at-20 target picker w/ taxonomy chip, Link in place, 5 code-specific refusals, empty state), SkuLocationOverlay (default-collapsed per-shop tri-state activation Inherit/On/Off + weekday/weekend par w/ "Inherit" placeholder, per-location independent Save + step-up), MiscSection (4 yes/no attestations, no column headers, per-row required error beside the toggle, optional note) and MixedPrepSection (numeric + yes_no + free_text in one card, no shared header strip, per-row inline column captions)
- FINDINGS: (1) linking rewrites item_id/vendor_item_id on the EXISTING line — id and LABEL preserved, so a mislabelled line stays mislabelled and needs a separate builder edit; (2) "Inherit global" is materially different from On (inherit keeps following later global changes, On opts out) and blank par shows "Inherit" not 0 — same em-dash-vs-zero discipline as credits; (3) all 4 Misc yes/no answers block submit, error rendered beside the toggle not in a top summary; (4) MixedPrepSection REPLICATES MiscSection's yes/no row verbatim because MiscRow is module-private — source comment asks for hand lockstep; genuine duplication to name in the handoff
- STILL OWED: UserMenu, ManagerLoginForm, IdleTimeoutWarning, UnifiedSearchResults (auth + chrome) — then the mobile pass
- Added 32a: three route-less islands. LtoClient (surplus->LTO/discount: perishable surplus lines from the catering forecast, per-line inline form w/ LTO|Discount toggle, percent required for discount, optional featured price, default window = today+2d, choice lines greyed w/ "choice — no action"; manage list w/ kind+status pills and Cancel on active; step-up on create AND cancel). CreditResolveControls (every credit always visible, AGM-only Resolve -> inline second-tap confirm w/ Credit | Refund | Write off + optional note; money law: null amount renders em dash, never $0.00). ProductionConsumptionPanel ("Uses: …" collapsed summary -> per-SKU rows w/ Case/Each toggle, oz always shown alongside, stored value is ALWAYS oz, typed unit remembered only for read-back, no-conversion SKU loses its toggles)
- FINDINGS: (1) em dash vs $0.00 carry different meanings and both appear — "we don't know what we're owed" vs "we're owed nothing"; (2) THREE mirrored gate constants now (CREDIT_RESOLVE_MIN 6, RECEIPT_MIN 4, RECEIPT_OVERRIDE_MIN 6) each duplicated into a client island because the real ones are server-only — the spec should name one client-safe constants module instead of repeating the keep-in-sync comment; (3) the LTO list is a forecast-driven queue, not a marketing tool, matching Juan's stated model
- STILL OWED: NeedsLinkQueue, MixedPrepSection, MiscSection, SkuLocationOverlay, UserMenu, ManagerLoginForm, IdleTimeoutWarning, UnifiedSearchResults — then the mobile pass
- SCAN #4 — all 186 components enumerated and probed (not just routes). VendorClaimPanel is now built (31a). CONFIRMED STILL OWED, 10 surfaces: components/admin/catering/lto/LtoClient.tsx (19.5KB), components/receiving/CreditResolveControls.tsx, components/production/ProductionConsumptionPanel.tsx, components/admin/templates/NeedsLinkQueue.tsx (7.8KB), components/prep/sections/MixedPrepSection.tsx (13.9KB) + MiscSection.tsx (8.9KB — false-positive earlier: the "Misc" hits are a SKU class in 13a and "Miscounted" in 6a, not the prep section), components/admin/skus/SkuLocationOverlay.tsx (7.8KB — false positive earlier: every "overlay" hit is 29a's Shift Overlay), components/UserMenu.tsx, components/auth/ManagerLoginForm.tsx, components/auth/IdleTimeoutWarning.tsx, components/reports-hub/UnifiedSearchResults.tsx
- Confirmed COVERED by tight probe: SalesTab (6a), ItemQuestions/ItemRow (10a/13a), PackChainWizard + SkuPackChainPanel + SkuCostPanel + SkuCatalogClient (10a/13a), SkuBuilder (20a "Portions"), SectionsTab + ParGrid (11a), LocationChecklistTab (10a), AmPrepForm (1a/5a/11a), VendorDetailClient (11a/13a), OpeningPrepEntry (3a), ChecklistItem (3a/4a), NotificationList (18a), WrittenReportForm (9a/13a), EquipmentDetail (22a), CapacityClient/FaqClient/FulfillmentClient/MenuClient/ToastTab/PackagesClient/PrepDemandClient/PricingClient/ZonesClient/RecipeBuilder/TemplateBuilderClient/users + catering clients
- Added 31a: VendorClaimPanel, both shapes. LINKED = side-by-side "What we counted" vs "Vendor claim" + attestation bar (Matches / Discrepancy / AGM-only Override w/ required reason, terminal + override_locked line). UNLINKED = multipart attach (15 MB cap, .pdf/.eml/image/.txt/.html) + default-collapsed "Link an existing receipt" triage queue w/ per-row PO chip or "No order matched", Attach-to-an-order lazy candidate picker, and Parse-now / Retry parse. Counted column reconciles with 27a (prov 2, mayo 1, rolls 3 short, capicola 0)
- FINDINGS: (1) untrusted-content law — image/PDF inline from signed URLs, text/html/.eml are LINK ONLY, sign-failure renders "unavailable" not a broken frame; (2) RECEIPT_MIN 4 and RECEIPT_OVERRIDE_MIN 6 are mirrored client constants with a keep-in-sync comment — they will drift; (3) delivery-link and PO-attach are INDEPENDENT axes, so "linked" is ambiguous without saying linked to what; (4) the nightly sweep only parses EMAILED receipts, so hand-uploads wait for a manual Parse-now, nothing schedules a retry, and no count of unparsed rows exists anywhere
- STILL OWED after 31a: CreditResolveControls, ProductionConsumptionPanel, LtoClient (operational), then UserMenu / ManagerLoginForm / IdleTimeoutWarning / UnifiedSearchResults (auth + chrome), then the mobile pass
- EXHAUSTIVE SCAN #3 (route-level + component-level). ROUTES: 83/83 covered (app/page.tsx out of scope by user decision). But a COMPONENT-level scan found 8 real UI surfaces that no route probe catches, because they are islands/modals inside covered routes: components/receiving/VendorClaimPanel.tsx (34KB), components/receiving/CreditResolveControls.tsx (8.5KB), components/production/ProductionConsumptionPanel.tsx (10KB), components/admin/catering/lto/LtoClient.tsx (19.5KB — 5a/17a describe LTOs conceptually, the editor was never read), components/UserMenu.tsx (12.8KB), components/auth/IdleTimeoutWarning.tsx (7.3KB), components/auth/ManagerLoginForm.tsx (11.4KB), components/reports-hub/UnifiedSearchResults.tsx
- METHOD NOTE: route-name probes and single-word text probes BOTH produce false passes (e.g. "claim" matched companies' "Claimed", "consumption" matched prose about consumption records). Only distinctive multi-word markers from a component's own strings are reliable
- 30a public profile relabelled from the real profile.* namespace. Two corrections beyond wording: profile.velocity_note is "Days active over the last 30 days." (the BarChart is a 30-day ACTIVE-DAY series, not tasks/day — redrawn as 30 bars), and profile.ratings_title is "Manager ratings — mostly Great 🌟" (a 4th source emoji, so the ratings header itself is celebratory copy, not a neutral label). Ana's MVP tile corrected 2 -> 1 to match locked 9a's directory star
- PATTERN worth stating in the build spec: three separate namespaces describe the same person — me.* (self view, 2nd person), people.* (manager's view of someone), profile.* (public celebratory card). Strings overlap in meaning and differ in wording and person; do not reuse across surfaces
- 30a person detail relabelled from the REAL people.* namespace (people.detail.* / people.signals.* / people.on_track|needs_attention / people.weekday.*) — I had lifted 9a's me.* self-view strings, which made a manager's view of Marco read "How managers have rated you" and "what you actually filed". people.detail.gradients = "PM gradients", streaks_from_data = "from our data", streak_best = "personal best / day", toast_note = "Clock-in / early-arrival streaks need Toast", tenure = "{n} mo"
- 30a public profile now renders the pin / star / flame emoji PublicProfileCard actually renders, so the aria-hidden finding describes the drawn frame rather than contradicting it
- AUDIT CORRECTION #2: last turn's "route coverage complete" was WRONG. A rescan of all 83 app/**/page.tsx against the document found FIVE built surfaces never drawn: /operations/counts, /operations/production, /order/account, /profile/[userId], /reports/trends/team/[personId]. 9a says "tapping one opens that person's public profile" but never drew it; 9a's "per-person" prose is about directory tiles, not the AGM+ detail page
- Added 30a (recreations, not proposals): Inventory Audit (disjoint full+loose lines, partial fraction, incomplete-line warning, step-up on record; On-hand panel with the 3-tier provenance chip Audited/Par-pass/Inferred, weight rows = variance short/over, count rows = "used or lost" / "counted over by N", stale-anchor warning), Production (SKU->item conversion, predicted output prefilled only when empty, recent log), Your orders (customer account: status badge tones, Review & pay vs View, Saved details, empty state), Public profile (positive-by-contract card), Person detail (score + health verdict, the read, empty AI slot, contribution + on-time sparks, 3-way gradient incl. needs-work, streaks, signals)
- FINDINGS: (1) counts submit() silently drops partially-filled lines — the warning names a COUNT, not which lines; (2) two gap vocabularies with INVERTED sign conventions between weight and count rows, which is why "counted over by N" is worded instead of signed; (3) "Order again" routes to an empty builder (reorder prefill deferred in source) — the only portal label that promises what the click won't do; (4) saved details are read-only with no edit path anywhere in the portal; (5) PublicProfileCard's pin/star/fire emoji lack aria-hidden, so a screen reader announces "round pushpin" before the shop code
- ROUTE COVERAGE: now genuinely complete — 83/83. Remaining = mobile pass, then polish, then the build spec
- Added 29a: the 7 remaining PlaceholderCard stubs as PROPOSALS (marked as such on every frame) — Prep Sheet (par − on-hand, hand-tune w/ computed value retained, single-submission lock), Shift Overlay (level 5/6/7 sections present, 9/10 absent not locked, 3h edit window then append-only), Today's Synthesis (drillable rollup), Tip Pool (pool ÷ hours = rate, per-employee rows, draft/calculated/distributed), Customer Feedback (1–5 + category + comment + follow-up assignee + loop close-out), AI Insights (threshold breaches, GM+/Owner+/CGS tiers), Recipe Flash Cards (batch scaler rewriting quantities, method, photo slots)
- Built ONLY from each stub's declared *.ph.* i18n (title/description/features, both languages) — app/recipes uses recipe_cards.ph.* deliberately to avoid colliding with the shipped admin recipes.* namespace
- FINDINGS: (1) BOTH en.json and es.json carry a mojibaked ">=" in ai.ph.feature.alerts ("voids ?1.5%, comps ?3%, cash short ?$10") — user-visible on the stub today, worth a whole-file sweep for the same damage; (2) the cash rule is asymmetric (fires on short only), so an unexplained drawer OVER passes silently; (3) Synthesis and Feedback overlap surfaces that already exist (7a's roll-up, catering Insights' feedback panel) and neither stub knows it; (4) prep's single-submission lock needs the overlay's append-only-correction idiom rather than a second unlock mechanism
- ROUTE COVERAGE COMPLETE: every built surface recreated, every stub proposed. Remaining = the mobile pass, then polish on the highest-traffic surfaces, then the build spec
- Added 28a: PoPanel — draft (walker stepper rows, qty 0 = struck-through kept line, Save + second-tap Confirm), confirmed (frozen line table, dormant auto-email block, manual tier transmit w/ contacts + order methods + always-present Copy, Mark placed), trail (three-way match w/ billed-over-received + unmatched invoice line + em-dash null legs, timeline, ack evidence, transmissions, inbound SMS, delivery link, credits pill, Mark reconciled), the Mark-placed dialog, and the open_credits / confirmed_during_edit refusals
- 27a credit re-pinned to CAP-0809-SYS at 3d old (was CAP-0811-SYS at 1d) — 26a states Sysco's oldest open credit is 3d and 25a lists CAP-0811-SYS as reconciled, which reconcile forbids while a credit is open
- FINDINGS: header comment says AGM+ may reconcile but code is actorLevel >= 5 (shift lead); Confirm silently PATCHes pending edits first, so an unsaved qty enters the snapshot; transmit.emailOrderingAvailable reads false everywhere (verified sending domain outstanding) so the auto block has never rendered, and INBOUND invoice parsing has no availability flag at all; empty billed column is ambiguous between no-invoice and pre-sweep, disambiguated only on placed/invoiced by naming the ~5:45 AM cadence
- Added 27a: the door ceremony — ReceivingForm mid-count (draft resume banner, PO banner, saved-on-device pill, collapsed tap-tick expected rows, one expanded stepper row w/ auto-suggested Short, offered row, vendor-scoped usage-ranked Add picker, receipt photo + Photo later, collapsed "Makes up a short?") and the MISSING-ITEM HONESTY GATE armed (per-row Received / Didn't arrive, short-claim summary, "Complete anyway" button flip) + the duplicate-409 strip
- 27a re-grounded on IntakeLineRow.tsx + PhotoCapture.tsx + verbatim receiving.door/missing/makeup/flag strings: 3 collapsed row states (confirmed = co-success surface + white tick; expected = gold tick 48x56; offered = dashed em-dash box, NO tick, "Tap to count"), expanded row gains Level picker + unit price + observed oz/each + note + per-line PhotoCapture, suggested flag is DASHED danger w/ a dot ("Suggested: Short. Tap to confirm."), duplicate-409 is WARN-toned (danger is reserved for a failed submit) w/ a co-cta link, "Done" not "Collapse", "Tap again to confirm" not "Complete anyway"
- 27a: offered rows and a PO prefill are MUTUALLY EXCLUSIVE (source==="po" means a template came back, so every row is seeded expectedQty!=null, offered:false; offered rows come only from fallbackLines() when !tpl) — the offered/em-dash state is described in prose, not drawn beside the PO banner. All receiving.form.* labels now verbatim: "Delivery date", "Invoice total ($)", "Unit", "Price/pack ($)", "Oz/each (opt)", "Pick a SKU…", "+ Add item", "Line note (optional)", "Add photo"
- SOURCE CONTRAST BUG: the armed submit is bg-co-gold-deep + text-white = 2.41:1 at 13px/700 uppercase — the primary action in that state. Drawn one step darker (#8A6D1F, ~4.9:1). Same rule as the rest of the doc: #8A6D1F when gold-deep carries text, #C9A227 for fills and borders only
- FINDINGS: template SEEDS qty at expected so an uncounted row files as fully received (the gate is advisory — 2nd tap files anyway); shorts are PO-gated (missing_requires_po) so a last-delivery prefill claims nothing; "Photo later" writes literal [PHOTO PENDING] into the note instead of a boolean, so pending-receipt deliveries aren't queryable; the offline draft key is per LOCATION (coops.intake.draft.<locationId>) so two same-hour deliveries share one draft and the resume banner shows only a time
- 26a extended with the DELIVERY DETAIL (app/(authed)/operations/receiving/[id]/page.tsx): header (vendor, date, #invoice, "by {name}"), the PO thread as a link to /ordering?location=&po= , invoice total, notes, "Receipt attached", header badges, and per-line rows with the four discrepancy flags (Short / Over / Damaged / Sub), "{price}/pack" or "no price", resolved oz, photo link, notes
- FINDINGS: (1) the back link must carry ?location= because the receiving list redirects to the dashboard without one; (2) "Continue intake" is only a note + link — seeding the form with already-counted lines is explicitly deferred in source, so the second half of a split unload is retyped; (3) unit price renders "no price" rather than $0.00, same money law as the credits panel
- STILL OWED: components/receiving/{ReceivingForm,VendorClaimPanel,CreditResolveControls}.tsx, components/ordering/PoPanel.tsx, the 7 PlaceholderCard stubs, then the mobile pass
- 26a extended with the open-credits panel, built on components/receiving/OpenCreditsPanel.tsx: default-collapsed CollapsibleSection, verbatim receiving.credits.* ("Open vendor credits" / "{n} open" / "Aging" / "oldest {n}d"), AGING_DAYS = 7 puts an Aging pill on both the aging vendor row AND the collapsed header, and MONEY LAW — a null totalCents renders an em-dash, never $0.00. With no open credits the panel renders nothing at all (contrast: Today's orders shows a watermark)
- STILL OWED: components/receiving/ReceivingForm.tsx + app/(authed)/operations/receiving/[id]/page.tsx (the door ceremony + delivery detail), components/ordering/PoPanel.tsx, the 7 PlaceholderCard stubs, then the mobile pass
- Added 26a: /operations/receiving — the delivery LIST. Verbatim receiving.page.* / receiving.badge.* strings; rows show vendorName + purchaseOrderCode (absent on a walk-in with no order) + date, badges only when unresolved, then "{n} item(s) · #invoice · receivedByName"
- AUDIT CORRECTION: receiving was wrongly marked covered in the earlier route audit. It appears elsewhere in the doc only as MENTIONS ("Receiving logged" in 12a adoption, "Mid-shift · Receiving" in the roll-up, Baldor "3 short deliveries" in 11a, the PO-2402 comms thread in 8a) — the screens themselves were never drawn
- FINDINGS: (1) "Missing email" is derived per request, not stored — complete + counted_only + no emailReceiptId + older than a 48h grace; the clock is read once outside the render so the flag can't drift mid-page, which means the badge appears on its own two days later; (2) only "Matched" reads settled — the two neutral badges (In progress, Overridden) are states, the three warn ones are actionable; (3) receiving.page.line_count is "{n} item(s)", the same bracketed-plural wart as the quote builder's "{count} line(s)"
- STILL OWED: components/receiving/{ReceivingForm,OpenCreditsPanel}.tsx + app/(authed)/operations/receiving/[id]/page.tsx (the door ceremony + delivery detail), components/ordering/PoPanel.tsx, and the 7 PlaceholderCard stubs
- 25a empty state rebuilt on components/EmptyState.tsx (flex column, items+justify center, gap-3, text-center, py-16 overridden to py-6 by the caller's className; BrandMark variant="watermark" at markSize 40 vs its 64 default; message text-sm font-medium text-co-text-muted; watermark is decorative/aria-hidden)
- OPEN, now stated on 25a: components/ordering/PoPanel.tsx — the overlay that owns confirm / place / reconcile, the PO_MIN gate and the ?po= deep-link target — is the last component not yet designed
- Added 25a: OrderingSurfaces, the second /ordering island — Today's orders, Cutoff-today with Generate draft, and collapsed Order history. Three phone frames (populated / history open / pre-draft empty). Verbatim ordering.po.* strings; PO lifecycle draft -> confirmed -> placed -> received -> reconciled plus invoiced, mapped to AlertPill tones info/warn/ok/ok/ok/info
- FINDINGS: (1) a cutoff vendor that already has a draft leaves the Cutoff queue and appears in Today's orders instead — two lists, never both; (2) confirmed is the ONLY warn state, so an agreed-but-unsent order is the single thing asking for a hand; (3) generating a draft disables every button on the card, not just its own, with no reason shown on the other rows; (4) ?po= is a fetch key only — PoPanel re-runs the level gate + location lock server-side; (5) ordering.po.history_count is literally "{n}", the one collapsible in the app whose count is a bare number
- ROUTE COVERAGE: /ordering fully CLOSED (both islands). Remaining = the 7 PlaceholderCard stubs as proposals
- 24a: cutoffSoon now sits on the NEARER deadline (Sysco 2:00 warn, Baldor 4:00 info) — matching 4a/6a/11a where Sysco's 2:00 is the cutoff to beat; weekend-par badge corrected to 11px/700/0.08em per page.tsx; SkuRow par badge to 12px per ParPassWalker
- TOKEN NOTE for the spec: text-co-gold-deep is rendered #C9A227 where decorative (eyebrows, the ★ glyph) and #8A6D1F where it carries small text (badge labels), because #C9A227 measures ~2.4:1 on the cream surfaces. One of the two should win in the real theme — the accessible choice is #8A6D1F for text and #C9A227 for fills/borders only
- 24a pills rebuilt on components/ui/AlertPill.tsx: borderless, 11px bold, rounded-full px-2 py-0.5, gap-1. Tones map to tokens — warn = warning-surface/warning, danger = danger-surface/danger, ok = success-surface/success, info = surface-2/text-dim (NEUTRAL, not blue as first drawn). `uppercase` DEFAULTS TRUE, so "Order day" and the "Check" shrinkage badge render uppercase with 0.08em tracking while "Cutoff {time}" and "{n} marked" pass uppercase={false} and stay sentence case. The advisory-paused banner keeps its co-info border/background — that one IS blue in source
- 24a strings corrected to verbatim after a wider i18n search: page title is "Ordering walk — {date}" (not "Par pass"), shrinkage badge is "Check" (not "Shrinkage"), title "Par-pass differs from computed on-hand for {n} SKUs", help "The walk sees less than deliveries and sales predict — possible unrecorded waste. Advisory only.", sources audited / par-pass / inferred
- 24a delivery rows rebuilt on components/ordering/delivery-affordances.tsx: DeliveryRow = method label + value (truncated) left, action + Copy right. Verbatim actions "Email order" / "Open portal" / "Call"; method labels Email / Order link / Vendor portal / Phone / Contact; CopyButton cycles Copy -> Copied -> "Copy blocked" when the clipboard is denied. SECURITY NOTE for the spec: a navigable link renders ONLY when the vendor value passes a conservative validator (isValidEmail rejects ? # & % : and whitespace to stop mailto header injection; isValidHttpUrl requires http/https; isValidTel needs >=7 digits) — otherwise the row degrades to Copy-only. mailto subject+body are URL-encoded
- OPEN: /ordering mounts a SECOND island, components/ordering/OrderingSurfaces.tsx (today's orders, PO history, cutoff attention, ?po= deep-link) — not yet designed. 24a now states the deferral on the frame
- Added 24a: /ordering — the par-pass shelf walk, three phone frames (walk / review overlay / recorded). Built on components/ordering/ParPassWalker.tsx + app/ordering/page.tsx with verbatim ordering.* strings
- Model: ONE input per par'd SKU (order qty to reach par). EMPTY = not observed, never submitted; EXPLICIT 0 = "We're full · 0", submitted, anchors on-hand at par. A per-SKU `marked` flag tracks observation independent of value. Chips: "Suggest {n}" (only when an on-hand anchor exists), "Empty · order {n}" (ceil full par), "We're full · 0", "Clear". Vendors are CollapsibleSections, defaultOpen = isOrderDay, badges = Order day / Cutoff {time} (warn when cutoffSoon) / {n} marked. Sticky bar "{n} SKUs marked · Review order"; review shows lines only because delivery affordances are server-side; DONE renders authoritative per-vendor cards with the PO code + one DeliveryRow per ordering method, each with Copy
- FINDINGS: (1) a SKU with no order-unit→oz conversion (canImplyOz false) can never earn a Suggest chip — the row says "No weight set" in muted text, same unpriced/unlinked SKU family 10a surfaces; (2) the advisory-blackout banner exists because the designed pre-5AM silence was read as a regression — good precedent for explaining absent data; (3) a failed draft-PO does NOT lose the walk (observations save, codes regenerate) — the right ordering of concerns; (4) shrinkage notices are advisory only, naming SKUs without correcting counts
- ROUTE COVERAGE: /ordering CLOSED. Remaining = the 7 PlaceholderCard stubs (tips, ai, operations/prep, operations/overlay, operations/synthesis, feedback, recipes) as proposals, plus app/page.tsx storefront (out of scope by request)
- CREDENTIAL MODEL (from Juan, corrects an earlier reading): below manager level the PIN is the primary credential — set by the manager who onboards the employee and MATCHED TO THEIR TOAST PIN so one number works in both systems. The password is supplemental (secures the account, doesn't open it) and only becomes the way in at manager level. So the reset route sending nothing below level 6 is by design, not a hole. Residual gap: the constant-200 response also hides "you don't use one of these" from anyone who tries, and no surface tells staff a forgotten PIN goes to a manager
- BUILD-SPEC REQUIREMENT: PIN set at onboarding must mirror the Toast PIN — implies collision handling across staff and a visible PIN field in user management (12a shows role change but not PIN assignment)
- 23a corrected: both password fields dim at 50% in the mid-flight card (SetPasswordForm gives BOTH inputs disabled={submitting} + disabled:opacity-50, same as the button), and the reset-gate finding was rewritten after reading app/api/auth/password-reset-request/route.ts
- FINDING (grounded, replaces an inferred one): requesting a reset is OPEN to anyone, but an email is only sent when ROLES[role].hasEmailAuth — false below level 6. The route ALWAYS returns a constant 200 { ok: true } to prevent email enumeration, so a shift lead gets the same success response, no email arrives, and the real disposition is written only to the audit log (outcomes: user_not_found / user_inactive / role_not_email_auth / email_not_verified / email_sent / email_failed / insert_failed / throttled). PIN-based staff need a different recovery path. Reset tokens expire in 1 HOUR; requests are throttled 3 per 15 min per email; per-IP limiting is deliberately deferred
- Added 23a: /verify and /reset-password, six frames — set-password resting, field-error, transient-toast + dead-end, reset resting, mid-flight spinner, success card. Built on components/auth/AuthShell.tsx (mustard band w/ wordmark + OPERATIONS, max-w-md focused column) and SetPasswordForm.tsx (2 fields, 8-char client min matching server, one error at a time, error clears on that field's next keystroke, spinner replaces the label while submitting). Verbatim auth.verify.* / auth.reset.* / auth.set_password.* strings
- FINDINGS: (1) an "account_inactive" failure is rendered on the PASSWORD field, so a deactivated account reads as a password problem; (2) both pages hard-code TranslationProvider initialLanguage="en", so a Spanish-speaking new hire's first screen is English though every string is translated — the invite link could carry the language; (3) reset's dead end offers a next action ("request a new one") while verify's says contact your manager — correct asymmetry, worth keeping; (4) reset is documented level 6+, so a floor employee who forgets a password must be re-invited, and neither page says so
- 22a maintenance note form rebuilt on app/(authed)/maintenance/maintenance-client.tsx (AddMaintenanceNote): verbatim maintenance.add_note.* — "Log a maintenance note" / "Equipment" / "Other…" / "Equipment name" / "What's going on?" / "Save note" / "Saving…". The select is ALWAYS pre-selected (defaultEquipmentId, else equipment[0], else Other) — there is no placeholder option; picking "Other…" reveals a free-text name field. Note is the only required field; the form mounts on BOTH the overview and detail views
- FINDING: choosing "Other…" POSTs equipmentId null + otherLabel, so the note belongs to no unit — it appears on no detail page and in no equipment history
- Added 22a: the three shipped-but-undesigned surfaces from the route audit — Cash Deposit (entry + signed read-only + closing-finalized lock), Maintenance Log (overview + equipment detail), Settings
- Built on the real components after a first pass drew them from imagination: lib/cash-shared.ts (deposit = drawer − float; overShort = deposit − projected; DEFAULT_FLOAT_CENTS 20000; CASH_REPORT_BASE_LEVEL 4; DENOMINATION_UNITS_CENTS = 10 units 10000..1, label = cents>=100 ? `$n` : `n¢`), components/cash/DenominationCounter.tsx (grid-cols-2 sm:3 of label + number input — NO steppers, NO coin row; "Drawer total {amount}" beneath), components/maintenance/TempTrendChart.tsx (SVG 280x64, area gradient, dashed co-cta safe-max line, polyline, one dot per reading green/red vs safeMaxF), components/maintenance/EquipmentDetail.tsx (status chip, "Safe ≤ {max}°F", chart, 6-stat grid Latest/Today AM→PM/Min/Max/Avg/Out-of-range, readings timeline w/ AM-PM badge, history w/ "{name} · {time}" + "from checklist" when byName is null), lib/maintenance.ts FRIDGE_DEFAULT_SAFE_MAX_F = 41, components/settings/LanguageSettingCard.tsx (radiogroup, pessimistic commit, "Saving…" mid-flight)
- FINDINGS: (1) cash "Projected (from Toast)" is a hand-typed number input though the nightly sync already lands the day's cash sales; (2) the $200 float is a shared constant — a shop on a different float is wrong every night AND the server recomputes with the same rule, so it can't catch it; (3) freeform on-shift names save with a null user id; (4) fridge status is computed on read only — ten readings above 41°F raise nothing, unlike under-par prep which notifies; (5) /maintenance is the ONLY gated page that renders an explanatory card below level instead of redirecting; (6) a maintenance note captured during closing has no author and shows "from checklist"
- Added 21a: platform role chips (Toast = menu & sales, EZCater = orders in), an "Add a platform" slot per location, and the answer to which integration ingests orders: EZCater writes pipeline leads from a signed webhook; Toast only maps the menu so the nightly sales pull reconciles
- ROUTE AUDIT of all 83 app/**/page.tsx — see "## Route coverage" below. Three surfaces are BUILT but never designed (cash, maintenance, settings); one admin editor was designed only conceptually (catering LTO); the rest are either covered by 1a-21a or are PlaceholderCard stubs
- Added 21a: the Toast/EZCater crosswalk (the ToastTab surface). Per location: not-connected banner, Toast restaurant GUID + EZCater caterer UUID fields with their own save buttons, Run match / Check drift, the candidate confirm-or-reject queue with match scores and modifier dispositions (counts / subtracts / ignored / assortment), confirmed + stale mappings in ONE list with the "Gone from Toast" badge, the 5-bucket advisory drift report, and the EZCater webhook event log
- FINDINGS: (1) an EZCater order for a shop with no caterer UUID logs "unmapped_location" and is DISCARDED — no lead, no notification, visible only on this tab; (2) drift is a manual button with no schedule, so a price divergence stays undetected until someone presses it (the nightly Toast pull is an obvious host for a drift pass); (3) both id fields accept anything client-side and are validated only server-side
- Read components/ui/CollapsibleSection.tsx: the header is a FULL-ROW 44px button (title + count + badge grouped LEFT, aria-hidden chevron far RIGHT, aria-expanded + aria-controls={idBase}). Restructured every section head in 20a AND 17a's prep-demand day heads, which had the chevron drawn on the left with the badge pushed right
- Read components/admin/catering/faq/FaqForm.tsx: FaqFormValues = { locationId, questionEn, questionEs, answerEn, answerEs } — NO slug, NO displayOrder. Grouped "Question" / "Answer" headings over bilingual textareas (rows 2/2/4/4); location select renders only in add mode; canSubmit needs questionEn + answerEn, so the Spanish halves save empty
- FINDING sharpened: a FAQ's key and display order are printed on every row and sorted by, but appear in NEITHER the create nor the edit payload — set once by the server and unreachable from the app thereafter
- Added 20a: Catering Menu (MenuTabs Menu/Toast shell; CollapsibleSection per section w/ "{n} items" + "{n} available" badge, defaultOpen <=6; item rows w/ serves box + "Sizes (N)" disclosure + Catering/Only toggles; menu_item rows add the Portions toggle; inline SizeEditor Label/Price ($)/Serves add-edit-remove) and Catering FAQ (bilingual rows w/ Key + Order, Global badge, add form, Tier B create/deactivate + Tier A text edit)
- 19a piece model reconciled with the locked coverage bars: a 32-piece platter Serves 24 (14a "Mains 28 of 24" = platter@24 + 4 subs; 16a "Mains 54 of 45" = 2x24 + 6), 16-piece Serves 12, 8-piece Serves 6
- FINDINGS: (1) catering_portionable ("Portions") is the flag behind the piece model — an un-portioned sub can be sold whole but can never appear on a platter, and no screen says so; (2) catering_only implies catering_available, server-enforced; (3) an item_size carries an EXPLICIT dollar price while 17a's rate rules DERIVE one from menu_price — two mechanisms for the same number, neither editor mentions the other; (4) FAQ display_order is printed on every row and sorted by, but nothing in the client can change it (edit strips all but the 4 text fields); (5) FAQ location is create-only, same permanence as a package's location
- 19a Office Spread reconciled: a package line can only ref an item or menu_item (mutually exclusive, NO package option), so the Sub Platter sizes are packages in their own right and Office Spread names items instead. Sub Platter · 32 pieces added as a row at $285.00 (ties 16a's quote line). User wants package-in-package: worth building as a recursive line ref — quote lines already carry a packageId, only package lines can't
- Price advice now publishes the route's REAL output after reading lib/admin/catering/package-pricing.ts: fixed lines = derived unit x qty; CHOICE lines = average of eligible options x that line's quantity — and on a choice line the quantity is the PICK COUNT, so a 32-cover package's protein contributes only $29.90. alaCarte $237.90 vs a $643.00 package -> impliedRateBps 27028 -> impliedDiscountBps -17028 -> renders "Implied premium −170.3%" in amber
- FINDINGS: (1) choice-slot pricing uses pick count as a unit count, so every package with a choice slot gets an understated recommendation and the advice argues for cutting price on the packages that need it least; (2) discount and premium share one signed field, so the premium case prints a NEGATIVE percentage under a label that already says premium; (3) hasBasis is alaCarteCents > 0, so a global package with no preview location AND a package whose every line is unpriceable both render the same "Pick a location to preview pricing." message
- 19a REBUILT on verbatim admin.catering.{packages,capacity}.* strings + the three components I'd first drawn from imagination (SummaryRow, PriceAdvicePanel, CapacityPolicyForm). Corrections: pricing modes are per_head / per_platter / fixed ("Per head" / "Per platter" / "Fixed price") — there is a THIRD mode; global reads "Global (all locations)"; meta is "Min {count} people · {hours}h lead time · Serves {count} · {n} items"; line items section is "What's included"; unconfigured reads "Not configured"; SummaryRow's toggle is a LABELLED pill ("Show details" / "Hide details") not a glyph
- PriceAdvicePanel is discount-vs-premium, NOT cost-vs-shortfall: recommended à-la-carte value, then implied discount (green) or premium (amber) as a PERCENTAGE, an unpriceable-lines note, and Use recommended. It is itself collapsed by default ("Pricing advice") and fetches nothing until opened. A GLOBAL package has no rate basis, so it renders a preview-location selector and "Pick a location to preview pricing."
- FINDING added: price_advice.unpriceable is "{count} line(s) can't be priced." — a bracketed plural that reads "1 line(s)" in the common single-freeform-line case, same shape in es.json
- Added 19a: Packages editor (SummaryRow disclosure — collapsed dot-string meta + never-collapse Inactive badge and Edit/Deactivate in the badges slot; lazy drawer with PriceAdvicePanel + line items; fixed vs choice slots, choice options as chips with classic stars, three add modes fixed-picker / freeform / choice-slot) and Capacity & Blackouts (per-location card, 3 nullable caps with "No cap", Configure vs Edit, blackout add/deactivate)
- Cross-turn tie: Capitol Hill's 400-cover cap is what makes 15a's Sep 3 lead read "Limited availability · 55 covers left" (55/400 = 0.1375 < LIMITED_THRESHOLD 0.15)
- FINDINGS: (1) package field edits incl. PRICE are Tier A while create/deactivate are Tier B — repricing asks for less than hiding; (2) slug + location are identity fields and the edit PATCH drops them, so a package can never move location or become global; (3) a location's minLeadTimeHours and a package's leadTimeHours are unreconciled (Navy Yard 48h vs Light Lunch 12h); (4) capacity caps and blackout dates are advisory only — nothing refuses a booking on a blackout date; (5) freeform package lines can't be costed or counted for prep, only read
- 18a label sheet reworked (all PROPOSAL beyond the safety rule — today the page prints one fixed 2-up label per line, no stock/shape/granularity/logo): label-stock picker driving per-sheet count (4x2in 10-up default, 4x3.33in 6-up, 2in square 12-up, 2.625x1in 30-up DISABLED because a safety line will not fit), shape (square/rounded/circle, circle crops to the short edge), granularity per line/item/piece (5 / 15 / 169 labels; 169 at 10-up = 17 sheets, surfaced on the print button), brand logo on each label with a toggle, and piece/tray numbering
- Label copy switched to the VERBATIM catering.labels.* strings from lib/i18n/en.json: "Food Labels", "Print allergen labels for this quote's items.", "Qty", "CONTAINS" (uppercase), "May contain", "Allergen info not on file — verify before serving.", unnamed item = "Item". Sheet preview now shows the representative case (8 of 10 labels warning), since the source states most Wave-1 lines are ad-hoc and the warning is the norm
- Added 18a: notification bell (always visible, badge only when unread, 99+ cap), desktop dropdown + mobile full-screen sheet + empty state, rich under-par cards (accent stripe urgent=cta / info=gold-deep, detail + reason + italic freeText + LOCATION · TIME, cross-day gains a date in America/New_York), optimistic faded pending card, and the /catering/quotes/[id]/label print sheet (2-up, break-inside-avoid, contains / may-contain / verify-before-serving)
- MONEY CORRECTED across 15a/16a/17a from lib/catering/quotes-shared.ts computeChargeStack: the tax base is subtotal + serviceCharge (ALWAYS) + delivery (if taxOnDelivery) + gratuity (if taxOnGratuity). Brightline quote: tax 94.82, total 1042.98, deposit 260.75, balance 782.23; v3 1296.02 / deposit 324.01 / delta +253.04; customer review (napkins as a LINE -> subtotal 862, tip as the gratuity field) 1141.06 / deposit 285.27; 15a sent funnel 1481.88
- CLAIM RETRACTED: quotes snapshot their rates + stack at compute time, so a pricing-rule change CANNOT reprice a sent quote. 17a previously said the opposite
- 16a: added the 6th status "submitted" (in QUOTE_STATUSES, written by nothing), revision lands as DRAFT not sent, expired quotes refuse an accept while declined->accepted is allowed, 14-day default expiry
- FINDINGS: (1) NotificationCard only builds a detail block for under_par_alert, so opening_no_prior_data renders title+timestamp and its fully translated body never appears; (2) /api/notifications is a 501 stub and nothing polls — the bell only refreshes on dashboard render; (3) loadUnreadForUser filters delivery_method='in_app', so an SMS/email-only recipient sees nothing in the bell while the prefs UI is still "coming in a follow-up"; (4) unread-only means a mis-tapped card is unrecoverable — no read history anywhere; (5) previewQuote calls assertCanWrite, so even previewing a quote's math needs level 6
- Catering write floors READ, not inferred: lib/catering/quotes.ts QUOTE_READ_MIN 5 / QUOTE_WRITE_MIN 6, payments.ts PAYMENT_READ_MIN 5 / PAYMENT_WRITE_MIN 6, pipeline.ts PIPELINE_WRITE_MIN 6, customers 6, companies 6 — source comments name the level-6 role "catering_mgr+", so 15a/16a labels now read "Catering Mgr and up (level 6)". 16a's mark-paid button was wrongly gated GM+; it is level 6, and viewing payments is level 5
- 17a fixes: baseline nudge moved to a second location card whose rates are all 100% (the condition is rules.every(r => r.rateBps === 10000), so it cannot coexist with a 115%/130% override); suggested par corrected to par + ceil(demand) = 5; prep window re-dated to the document's own clock (Aug 12 + 14 = Aug 26) with days ascending and the Sep 3 Brightline lead called out as beyond the fixed horizon
- 17a rebuilt on the CLIENT components (not just the server page wrappers): added the Catering Rates surface (3 tiers location/section/item, bidirectional % <-> target-price input, 100%-baseline nudge, remove-with-confirm), Pricing & Tax redrawn as PricingClient's collapsed dot-separated read row + open form + in-place deactivate confirm, Prep Demand redrawn with its 4 tabs (Prep / Raw-SKU / Surplus / Toast sales), CollapsibleSection days, over-par pill carrying par+needed, "Raise par" LINK to /admin/pars, SKU rows w/ per-date oz + shortfall chip, surplus split perishable vs raw
- ROLE LABELS corrected from lib/roles.ts across 15a/16a/17a: shift_lead is level 5, so minLevel 6 = AGM / Catering Mgr / Prep Mgr / Social Media Mgr (4 roles on one rung) — every "Manager of Operations and up" on a level-6 floor was wrong
- TAX BASIS settled and cascaded: catering_pricing_rules.tax_on_delivery defaults TRUE, so Capitol Hill taxes food + delivery. 16a's quote tax 85.20 -> 88.00, total 1033.36 -> 1036.16, deposit 259.04, balance 777.12; revision v3 1287.50 (delta unchanged +251.34), deposit 321.88; customer review 1131.36, deposit 282.84; 15a sent-funnel 1475.06. 14a was already on this basis and is unchanged
- Added 17a: catering admin — Pricing & Tax (4 bps rates as percent + taxOnDelivery/taxOnGratuity toggles, configured vs unconfigured location, step-up + retire-not-delete, worked example reproducing 16a's quote), Prep Demand (14-day window, over-par + par bump, unresolved-choice / no-recipe counts, perishable surplus), Delivery Zones vs Fulfillment Zones side by side, and the 11-card sub-hub with per-card floors
- FINDINGS: (1) i18n carries BOTH admin.catering.hub.rate-rules and .rate_rules (hyphen + underscore) — one is dead; (2) pricing has flags for taxing delivery and gratuity but none for the service charge, so its taxability is decided in code; (3) pricing rules are undated — a rate change reprices quotes already sent at the old number; (4) delivery-zone minimums are advisory, nothing refuses an under-minimum order; (5) two GM-gated editors both named "zones" (fee/min vs map service area)
- 16a: Send now beside Save draft (password step-up), prep-issues panel in the builder (proposal: joins par counts + vendor cutoffs + capacity + unpriced SKUs to a quote), customer quote review = 14a step three lifted verbatim (counter reads 1 of 2), revision re-review as v3 with priced diff rows, payment progression sent -> accepted -> paid in full, and a four-origin matrix (portal / staff / marketplace / phone) with per-origin limits
- Added 16a: quote builder (customer-flow shape, no marketing; live server charge stack), quote detail (snapshot + lines + payment intents + step-up Send + Print labels), quote list grouped by status, and catering companies (domain auto-attribution + hand-attach)
- 15a quote funnel money re-based on 16a's quote totals (draft $612.40, sent $1,472.26 = 1033.36 + 438.90) — the funnel reads quote totals, not lead estimates
- FINDING: the staff quote builder has no coverage read; the at-a-glance panel on 16a's builder is a proposal, not in source
- Added 15a: catering CRM — pipeline board (6 stages incl. "Out for delivery", search-replaces-board, assignee/source filters, follow-ups due, capacity signal pill), insights (3 SQL-sum revenue cards + two funnels + feedback), customers grouped by company, and the hub with its GM+ admin cross-link
- FINDINGS for the build spec: (1) lib/catering/insights PIPELINE_STAGES lists only 5 stages — it omits "out", so an out-for-delivery lead counts in no funnel row; (2) PipelineClient receives a writeMin prop and ignores it, hardcoding canWrite = actorLevel >= 6; (3) the board prints raw ISO event/follow-up dates while insights formats the same fields via formatDateLabel
- 14a rebuilt as FOUR steps (start / build / review / quote): restored the At-a-glance coverage panel, corrected the deposit to 25% (was "half now") and the balance term to "pay by 48h before, or forfeit", added the /order/quote/[id] confirmation surface
- Added 14a: customer ordering funnel (start / build / confirm) in the storefront visual language, from app/order/{page,start}/page.tsx
- Added 13a: create surfaces — new vendor (order/delivery day sets, calendar colour), new SKU (pack-chain builder resolving to oz), new recipe (inputs/outputs/yield, EN+ES, inventory_only SKUs excluded from the ingredient picker)
- Added 12a: user management (canActOn-limited role picker, filters), taxonomy (categories + order types, MoO+ to add), audit log designed from its placeholder features
- Added 11a: checklist-template hub + closing builder (per-line input type, photo/gate/role settings), vendors with the ordering week
- Admin hub strings taken verbatim from admin.hub.* / readiness.hub.* (all 8 adoption labels are verbs: "Checklists confirmed", "Counts recorded", …; arrives = "Arrives in Phase 5")
- 7a over-par audit example moved off provolone so its on-hand quantity stays consistent with locked 1a/5a/6a
- Adoption card rebuilt from ADOPTION_SURFACES (curated 8, declaration order); no-price SKU example moved to garlic so parsley stays priced for the locked 5a LTO financials
- 10a reconciled against locked 5a: provolone priced $42.80/case ($0.54/oz, matching 5a's $214 overstock), no-price example moved to parsley, admin.section.* labels verbatim
- Added 10a: admin hub (readiness pills, cron health, adoption), SKU catalog, pack-chain builder, item routing dossier, pars
- Confirmed from lib/team-metrics.ts: scoreDeltaPct is a true percent, and wins.personalBest IS streaks.personalBest (peak tasks in one day, not a count)
- My Performance rebuilt on the real MyPerformanceData shape with verbatim me.* strings from lib/i18n/en.json (score label, on-time sub "not clock-in", toast_note, streak labels, wins pills, feedback reassurance)
- Added 9a: My Performance (own data, no rank, private notes excluded), Written Reports (append-only, visibility floor), team directory, customer feedback
- Added 8a: roll-ups, staff recipe cards, training matrix, announcements, comms — all five designed from their PlaceholderCard feature lists
- Added 7a: maintenance overview + equipment detail, inventory audit (count beside computed on-hand), and deep cleaning built from its placeholder feature list
- Read the report layer: reports-hub list/filter/detail, opening spot-check detail, checklist item input types
- Added 3a: input surface covering all five line types, opening report verify view, signal-first reports hub
- Added 4a: desktop dashboard and closing checklist at 1280 with sidebar nav + reference rail; breakpoint rules
- Added 5a: Trends (derived metrics only), Admin people + permission matrix, Catering and LTO
- Added 6a: Mid-shift Pulse (six panels in source order), Opening gate-blocked + count verification, Mid-day two-phase prep, PM report employee evals
- Sales band rebuilt with velocity (checks/hour vs a normal same-weekday curve), dayparts, refresh, and a catering-platform feed with order provenance
- CORRECTION: the captured ordersBulk fixture has NO time of day (businessDate is a bare int; orders/checks carry guid, voided, diningOption, selections only). Hourly/daypart is UNCONFIRMED — verify against a live payload before scoping
- Verified and usable: diningOption is on every order (do-catering / do-takeout / do-dinein) with a /config/v2/diningOptions fixture to resolve names
- Catering lane rebuilt on the real ezCater order shape (orderSourceType MARKETPLACE, headcount, catererHandoffFoodTime, customerTotalDue vs catererTotalDue, posItemId null = unmapped item)
- Reconciled the Pulse sales panel (two text lanes + top items, no chart) and fridge strip (chip row + maintenance link) against SalesPanel.tsx, lib/midshift-sales.ts, FridgeStrip.tsx
- Corrected 6a after reading the real sources: PM report is a wrap-up + MVP + 4-gradient employee eval (no sales chart); mid-day is phase1 count -> phase2 prep, multi-instance per day
- Documented the level-gates found in source (notes L5, cash L4) as visible redaction affordances

## Sync history
- 2026-08-16T04:51:21Z — role-scoped nav + live shared report tiles
- 2026-08-16T04:38:32Z — approved brand icon copied in place of a drawn mark
- 2026-08-16T04:37:31Z — initial read + refresh proposal

## Screen map
| Screen | Built from |
|---|---|
| PO panel (28a) | components/ordering/PoPanel.tsx (DraftView / ConfirmedView / TrailView / ThreeWaySection / ThreeWayRow / PlaceDialog; STATUS_TONE map, money(null) -> em dash, guide_position sort on assisted tier, isValidHttpUrl + isValidTel guards, second-tap arming reset on every reload), components/ordering/delivery-affordances.tsx (CopyButton / DeliveryRow / linkBtn), lib/po-match-shared (ThreeWayFlag + FLAG_TONE), verbatim ordering.po.* strings from lib/i18n/en.json |
| Toast crosswalk (21a) | components/admin/catering/menu/ToastTab.tsx (Tier-A step-up on every write via PasswordModal + pendingRef carrying busyKey; POST /api/admin/toast-map/match, GET /toast-map/drift?locationId, POST /toast-map/[mapId] {confirm|reject|unmap}, POST /toast-map/location {toastRestaurantGuid}, POST /api/admin/ezcater/location {ezcaterCatererUuid}; matchStatus candidate|confirmed|stale, confirmed+stale rendered in one list; ModBadge tone remove=cta / ignore=dim / else gold with portionQty+unit; DriftPanel buckets priceChanged / renamed / missingOnToast / newOnToast / unmappedCo), lib/admin/{toast-map,ezcater-map}, verbatim admin.toast.* + admin.ezcater.* strings |
| Catering Menu + FAQ (20a) | components/admin/catering/menu/{MenuTabs,MenuClient}.tsx (MENU write = Tier A via PasswordModal + pendingRef retry; flags cateringAvailable / cateringOnly / cateringPortionable / serves; MenuSection wraps CollapsibleSection, defaultOpen rows<=6; SizeEditor POST /menu/[id]/sizes, PATCH+DELETE /item-sizes/[id], dollars -> integer cents), components/admin/catering/faq/FaqClient.tsx (MIN 6; create Tier B, text edit Tier A stripping locationId, deactivate Tier B; row = questionEn + questionEs + answerEn + answerEs + slug + displayOrder), verbatim admin.catering.{menu,faq}.* + admin.toast.tab_* strings |
| Packages + Capacity (19a) | components/ui/SummaryRow.tsx (co-card, summary+badges left, labelled toggle button right w/ aria-expanded + aria-controls={drawerId}, drawer lazy on expanded), components/admin/catering/packages/PriceAdvicePanel.tsx (own open/closed disclosure; GET recommend?location=; PackagePriceRecommendation {alaCarteCents, impliedDiscountBps, hasBasis, unpriceableLines}; discount>0 green else premium amber; preview-location select only when locationId null), components/admin/catering/capacity/CapacityPolicyForm.tsx (3 IntFields in a sm:grid-cols-3, caps_hint, Cancel + submit right-aligned, empty string -> null) |
| Packages + Capacity (19a) — data layer | components/admin/catering/packages/PackagesClient.tsx (PACKAGE_MIN 6; SummaryRow + multi-expand Set; create Tier B / edit fields Tier A / deactivate Tier B / addLine+addSlotOption+removeSlotOption+setOptionClassic+removeItem Tier A; PriceAdvicePanel GET recommend lazily on expand; groupPickerItems -> optgroup by section), components/admin/catering/capacity/CapacityClient.tsx (CAPACITY_MIN 7; policy save Tier B create-or-PATCH-in-place; blackout add/deactivate Tier A, append-only active=false), app/admin/catering/{packages,capacity}/page.tsx, lib/admin/catering/{packages,package-pricing,capacity} |
| Notifications (18a) | components/dashboard/NotificationBell.tsx (44px trigger, badge min-w-20 h-5 bg-co-cta border-co-surface, 99+ cap, body-scroll-lock mobile only, Esc closes, focus returns to trigger), components/dashboard/NotificationList.tsx (fixed inset-0 sheet <=640px / sm:absolute sm:top-12 sm:right-0 sm:w-96 sm:max-h-[70vh] dropdown, card IS the read surface, optimistic pendingIds + useTransition, renderUnderParDetail only branches on under_par_alert, nyDateString cross-day compare in America/New_York), lib/notifications.ts (NOTIFICATION_TYPES, priority info/urgent, in_app/sms/email, loadUnreadForUser in_app-only two-step, markNotificationRead idempotent, formatNotification key->literal fallback), app/api/notifications/route.ts (501 stub), verbatim notifications.* strings from lib/i18n/en.json |
| Food labels (18a) | app/(authed)/catering/quotes/[id]/label/page.tsx (QUOTE_READ_MIN gate, 2-up grid + print:grid-cols-2, break-inside-avoid, PageHeader print:hidden), components/catering/quotes/PrintButton.tsx (window.print), lib/catering/quotes.ts loadLabelData (status listed / none_on_file / unknown; needsVerify = !info || status !== 'listed'; absence NEVER rendered as safe; allergen labelEn/labelEs follows the USER's language) |
| Catering rates (17a) | app/admin/catering/rate-rules/rate-rules-client.tsx (RATE_MIN 8; 3 scopes location/section/menu_item+item resolved most-specific-first; RateInput is bidirectional — pct mode shows cateringUnitPriceCents(regular,"whole",bps), price mode shows impliedRateBps; atBaseline nudge when every rule is 10000 bps; DELETE deactivates) |
| Catering admin (17a) | app/admin/catering/page.tsx (EDITORS registry: 11 cards + /catering cross-link, floors 6/7/8, surplus badge fails silently), app/admin/catering/{pricing,prep-demand,capacity,packages,fulfillment,zones}/page.tsx, lib/admin/catering/pricing.ts (PRICING_MIN 8, PRICING_ALL_LOCATIONS_MIN 9, bps 0..10000 recomputed server-side, ONE active rule per location via partial-unique index, deactivate never delete, Tier B step-up, audit on every write), app/api/admin/catering/pricing/route.ts, components/admin/catering/pricing/PricingClient.tsx (collapsed read row is one dot-joined line of 4 rates; fields live in PricingForm on edit; inline deactivate confirm; Tier B step-up on every mutation), components/admin/catering/prep-demand/PrepDemandClient.tsx (4 tabs incl. SalesTab; CollapsibleSection per day, defaultOpen when <4 days; choice lines get a needs-pick chip; over-par pill carries par+needed and a suggested-par LINK to /admin/pars — NOT an in-place bump; SkuRow per-date "Fri 100 oz" line + on-hand/order-more chip; SurplusTab splits kind=prep vs kind=raw_sku; portion prefixes 1/4 1/2), lib/roles.ts (shift_lead=5, AGM/catering_mgr/prep_mgr/social=6, gm=7, moo=8, owner=9, cgs=10), verbatim admin.catering.* strings from lib/i18n/en.json |
| Catering quotes (16a) | components/catering/quotes/QuotesClient.tsx (3 views in one island: list grouped by STATUS_ORDER w/ isExpired override on sent; builder w/ 400ms debounced /api/catering/quotes/preview — client NEVER computes money, package expands into per-item lines keeping packageId, zero-value stack rows not drawn; detail w/ PasswordModal step-up on send, PaymentsSection markPaymentPaid GM+ w/ window.confirm, /label in a new tab), app/(authed)/catering/quotes/page.tsx (pricingByLocation zones+rates+hasPricingRule; menuItems priced against locations[0] as an INTERIM rate basis) |
| Catering companies (16a) | app/(authed)/catering/companies/page.tsx, components/catering/companies/CompaniesClient.tsx (WRITE_MIN 6; domains as removable chips + add; contacts list + attachContactEmail; claimed badge = claimedByCustomerId; errors personal_domain / domain_taken) |
| Catering CRM (15a) | app/(authed)/catering/{page,pipeline,insights,customers}/page.tsx, components/catering/pipeline/PipelineClient.tsx, components/catering/customers/CustomersClient.tsx, lib/catering/{pipeline-shared (PIPELINE_STAGES incl. "out"),capacity (Mode-A signals + LIMITED_THRESHOLD 0.15 + dedup consumption model),intake-shared (LEAD_SOURCES registry)}, verbatim catering.* strings from lib/i18n/en.json |
| Mid-shift Pulse | app/(authed)/mid-shift/page.tsx, components/midshift/{AttentionBanner,SalesPanel,FridgeStrip}.tsx, lib/midshift, lib/midshift-sales.ts |
| Mid-day prep | app/(authed)/operations/mid-day/page.tsx, components/MidDayPhase1Form, MidDayPhase2Form, lib/prep |
| Maintenance | app/(authed)/maintenance/{page,maintenance-client}.tsx, components/maintenance/{EquipmentOverview,EquipmentDetail,TempTrendChart}.tsx, lib/maintenance |
| Inventory audit | app/(authed)/operations/counts/page.tsx, components/counts/{OnHandPanel,CountForm}.tsx, lib/counts, lib/counts-shared |
| Closing builder | app/admin/checklist-templates/closing/page.tsx, lib/admin/template-builder (loadTemplateBuilderView, runTemplateDoctor), components/admin/template-builder/{TemplateBuilderClient,PrepOverviewPanel} — same-day fills (es wording, spine link, GM+ step-up) vs structural edits published as a new version effective next operational day |
| Users | app/admin/users/page.tsx, lib/admin/users.ts (AdminUserListItem incl. lastLoginAt; changeRole/setLocations revoke all sessions), lib/roles (canActOn gates assignable roles) |
| Taxonomy | app/admin/categories/page.tsx, components/admin/vendors/CategoryListClient.tsx (label + slug + labelEs; inline add form, MoO+ Tier B step-up) |
| Audit log | app/admin/audit/page.tsx (PlaceholderCard — filter/highlight/diff/immutable) |
| Checklist templates | app/admin/checklist-templates/page.tsx, components/admin/templates/NeedsLinkQueue.tsx (count lines w/ no item or SKU; item+SKU picker, GM+ step-up, link preserves line id/label), lib/admin/needs-link-shared |
| Vendors | app/admin/vendors/page.tsx, components/admin/vendors/OrderingCalendar.tsx (7 weekday ROWS, vendor-colour chips, ● order / ★ delivery, no times), lib/admin/vendors (VendorWeekEntry: orderDays/deliveryDays sets) |
| SKU create | lib/admin/skus.ts (createSku fields incl. item_number, lead_time_days, weekday_par, weekend_par; NO price column), lib/admin/catalog-shared.ts (SkuClass = raw/packaging/cleaning/misc, default raw), lib/admin/cost.ts (loadCurrentSkuPrices — price is delivery-derived) |
| Vendor create | components/admin/vendors/VendorListClient.tsx (modal; requires >=1 category, >=1 order type, a first contact and a first ordering detail; Tier B step-up) |
| Order coverage | app/order/build/page.tsx (CoveragePanel + Bar + coverageHints/computeGapNudge: guests seeds from draft.lead.headcount — the 24→20 fix, never hardcoded; 4 bars Mains/Sides & chips/Sweets/Drinks; ONE nudge, biggest gap first; mobile bottom sheet w/ gold glow-pulse chevron; coverage is best-effort — category derived from section) |
| Order review | app/order/review/page.tsx ("Review, then lock in your date."; Event details, Your order, GTK.review, tip, napkins, server-authoritative charge stack via /api/portal/order/draft/preview; deposit 25% locks date & requirements; "Balance — pay by 48h before, or forfeit"; 4-step How payment works incl. Stripe + full refund if we can't accommodate + forfeit) |
| Order quote (confirmation) | app/order/quote/[id]/page.tsx (ownership-boundary not-found; snapshotted charge stack; pay panel heading/copy from paymentPlan mode: deposit_required / deposit_optional / pay-in-full), pay-buttons.tsx (options + amounts from server, provider deferred) — print action is NOT in source, added on request |
| Catering menu rows (step 2) | components/portal/StorefrontOrderTray.tsx (SizedRow = item name + one pill PER SIZE, keyed item+size; CompactRow for non-sized; catering box = isSized(i) || i.cateringOnly, singles follow; last:border-0) + lib/catering/menu.ts (items/menu_items/item_sizes are DB rows — sizes carry {label, unitPriceCents, serves}; catering prices DERIVED via cateringUnitPriceCents(menu_price, portion, rateBps) — there is NO hardcoded catalogue in the repo, so never invent trays/pans/prices) |
| Sub photography | components/portal/storefront-images.ts (SUB_IMAGE_MAP: exact-name key -> Toast S3 item URL for 9 subs; GENERIC_SUB_IMG spread photo is the FALLBACK only; subImage(name)) — sub names must match the map exactly ("Vesuvio II", not "The Vesuvio") |
| Order tray | components/portal/StorefrontOrderTray.tsx (dark band; subs as 4:3 photo cards w/ gold price pill + Add->stepper; sides/sweets/drinks as compact rows in 3 cols w/ gold headings; boxed "Catering portions" for sized/catering-only items, one Add per size; floating "N items · Start your order" pill; NO per-line allergen text) |
| Order packages | components/portal/StorefrontPackages.tsx (three marketing sections: platters photo + size options, lunch boxes, gold big-subs block; prices in co-cta; "Add to order" -> "Added checkmark"; leadTimeHours -> "48h/72h notice"; carry writes packageId references only, never a price) |
| Order companion | components/portal/GoodToKnow.tsx (auto-cycling strip, 5s, dots), components/portal/portal-content.ts (GTK.start / GTK.review, FOOD_FACTS, ITEM_FACTS, FAQ) |
| Order intake | app/order/start/start-client.tsx (magic-link intake: no password, geocode + draggable pin -> zone routing names the fulfilling shop, pickupNodes chooser, storefront carry-through, 30-min one-time link) |
| Order portal | app/order/page.tsx (storefront: packages + tray, groups Subs/Sides/Sweets/Drinks, 48h/72h lead times, delivery zones or free pickup, allergens per item, deposit link), app/order/start/page.tsx (locations, pickup nodes, mode=new/signin) |
| Create flows | app/admin/recipes/new/page.tsx (ingredient picker excludes inventory_only), components/admin/recipes/RecipeInputRow.tsx (two-step remove), components/admin/vendors/CategoryListClient.tsx |
| Admin hub | app/admin/page.tsx, lib/admin/sections.ts (ADMIN_SECTIONS: 11 sections, display order = derivation pipeline, minLevel gates, comingSoon stubs for pars/locations/audit), lib/admin/{readiness-load,ops-health} |
| SKU catalog | app/admin/skus/page.tsx, lib/admin/skus, lib/admin/cost, lib/readiness, lib/pack-chain-shared (isChainUnverified), lib/recipe-math |
| Items master | app/admin/items/page.tsx, lib/admin/{items,catalog} |
| Pars | app/admin/pars/page.tsx (PlaceholderCard — designed from stated features) |
| My Performance | app/(authed)/my-feedback/page.tsx, components/me/MyPerformance.tsx, components/trends/TrendControls.tsx, lib/pm-report.ts (loadMyFeedback), lib/team-metrics (MyPerformanceData: read/score/wins/contribution/onTime/gradientTally/streaks) |
| Team directory | app/(authed)/profile/page.tsx, components/profile/ProfileDirectory.tsx, lib/profiles.ts (DirectoryEntry = name/role/mvpWins only; leadership = level >= 8) |
| Written Reports | lib/written-reports-shared.ts (categories, visibility floor, 3h edit window, append-only) |
| Customer feedback | app/feedback/page.tsx (PlaceholderCard — Module #16) |
| Roll-ups / Training / Announcements / Comms / Recipe cards | app/{rollups,training,announcements,comms,recipes}/page.tsx (all PlaceholderCard — designed from stated features) |
| Admin recipes (existing, not redesigned) | components/admin/recipes/RecipesClient.tsx, lib/recipes-shared.ts, lib/recipe-math.ts |
| Deep cleaning | app/deep-cleaning/page.tsx (PlaceholderCard — surface designed from its stated features, not yet built) |
| Catering / ezCater | tests/fixtures/ezcater/orderByID.json |
| Toast sales ingest | lib/toast/{orders-shared,client-shared}.ts, lib/catering/toast-sales.ts, tests/fixtures/toast/orders-v2-sample.json |
| PM report | app/(authed)/pm-report/pm-report-client.tsx, lib/pm-report (Gradient, EmployeeEval, ShiftWrapUpRow) |
| Opening (gate + verify) | app/(authed)/operations/opening/page.tsx, lib/opening, components/opening/OverParModal |
| Dashboard | app/(authed)/dashboard/page.tsx, components/OpeningTile.tsx, components/CashDepositTile.tsx, components/receiving/ReceivingTile.tsx, components/DashboardNav.tsx |
| AM Prep | app/(authed)/operations/am-prep/page.tsx, components/prep/* |
| Par pass | app/ordering/page.tsx, components/ordering/ParPassWalker.tsx |
| Receiving | app/(authed)/operations/receiving/page.tsx, components/ui/AlertPill.tsx |
| Cash | app/(authed)/cash/*, components/cash/DenominationCounter.tsx |
| Closing | app/(authed)/operations/closing/* |
| Login / PIN | components/auth/PinKeypad.tsx, components/auth/AuthShell.tsx |


## Route coverage
Audited 2026-08-18 against all 83 `app/**/page.tsx`.

### Built, not yet designed — the real gap (cash / maintenance / settings CLOSED in 22a; verify + reset CLOSED in 23a)
| Route | What it is |
|---|---|
| app/(authed)/cash | Cash Deposit (Task 10). KH+ (CASH_REPORT_BASE_LEVEL), `?location=` required. Two states: read-only signed-off summary (projected / drawer total / over-short with sign-encoded readout keys / deposit / cash tips / on-shift staff) or CashClient entry form. Third state: up-front LOCK when today's closing is already confirmed — banner instead of a form that would 409. Ladder max-w-2xl→6xl; content composes at lg (count beside a tips/crew/submit rail). |
| app/(authed)/maintenance | Maintenance Log (Task 10). MAINTENANCE_BASE_LEVEL 3, `?location=` required, optional `?equipment=` switches overview → detail. Overview = fridge status + equipment list; detail = EquipmentDetail + AddMaintenanceNote. 14-day window for readings and notes. Below-level renders a maintenance.not_available card rather than redirecting. |
| app/(authed)/settings | User preferences. Deliberately thin: LanguageSettingCard (the only schema-backed self-serve pref), a link out to /profile, and an honest placeholder card for notification prefs (user_notification_prefs exists, no data layer). |
| app/admin/catering/lto | Designed conceptually in 5a (LTOs reconcile over-ordered / near-expiry stock) but components/admin/catering/lto/LtoClient.tsx (19.5KB) was never read — the editor itself is unmapped. |

### PlaceholderCard stubs — greenfield, no source UI to recreate
`app/tips`, `app/ai`, `app/comms`, `app/announcements`, `app/deep-cleaning`, `app/rollups`, `app/training`, `app/feedback`, `app/recipes`, `app/(authed)/operations/{prep,overlay,synthesis}`. All render `<PlaceholderCard title description features[] shippingIn>` from `*.ph.*` i18n keys. Where 7a/8a designed these (comms, announcements, training, deep-cleaning, roll-ups) the design is a PROPOSAL, not a recreation — worth stating in the build spec.

### Out of scope by decision
`app/page.tsx` (public storefront — user asked to leave it as-is), `app/verify`, `app/reset-password` (auth), `app/ordering` (surfaced via 6a's cutoffs; the page itself redirects below level 4 — see the NAV_LINKS finding).
