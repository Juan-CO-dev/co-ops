# Seed 38 — CC rulings on the review sheet (2026-09-20, night)

Juan: "Let's get it done tonight." These rulings turn `seed38-review.csv` into the seed's manifest. Nothing here writes to prod; the seed does, on Juan's word, after a sim dry-run + execute.

## By kind

| Kind | Rows | Ruling |
|---|---|---|
| `dedupe_level` (DL-*) | 55 (12 flagged) | **WITHDRAWN as a class.** Every active chain already has unique ordinals; the "duplicates" are `active=false` history that append-only correctly keeps. The seed touches no `sku_pack_levels` row for this reason. Where the ACTIVE level's quantity disagrees with the vendor export (Oregano `jug 96 oz` vs PFG `1/5 LB` = 80 oz; Onion Powder 80 oz vs 5 lb = 80 oz agrees; Thyme 7.52 oz vs 4 oz; Basil 23.2 oz vs 1 lb; Red onion 160 oz vs 25 lb = 400 oz), the seed **appends a new active level from the vendor pack and retires the old one** (`active=false`), citing the export line — ONLY for SKUs whose `item_number` matches an export row exactly. No fuzzy pack changes. |
| `price_conflict` (PC-*) | 8 | **Resolve from the vendor evidence, append-only.** For each SKU with two prices on one date, append ONE superseding `vendor_price_history` row, `effective_date` = the export's generated date (2026-09-18) or the receipt date, `unit_price` = the export/receipt price expressed at the SKU's purchase root (the basis below), citing the line. Rows with evidence: Arugula 242470 ($19.27 / 2/2 LB case), Canola Oil 589183 ($40.45 / 35 lb), Ham 231710 ($2.7263 per lb), Oregano 261432 ($55.27 / 5 lb), Shredded Mozz 288533 ($81.89 / 6/5 LB), Pepperoni (Delmar 558: $4.99 per lb), Black peppercorn (already current — no row). **Ever Roast Chicken: HOLD** (no vendor line on any document); the two same-date rows stand and the latest-price reader's tiebreak (recorded_at DESC) governs until a receipt arrives. |
| `price_basis` (PB-*) | 92 (9 flagged) | **Mechanical rule, no guessing:** for each priced SKU with an export/receipt price, compute the basis under which `catalog unit_price × (vendor pack contents in that basis)` reconciles to the vendor's billed price within 3%: try `per_case`, then `per_each` (one inner unit of the vendor case), then `per_lb`, then `per_dozen`, `per_bundle`. First that reconciles wins; **none reconciles → `price_basis` stays NULL and the row is listed in the dry-run as `basis_unresolved`** (that list is the next data errand, not a guess). SKUs with no vendor evidence at all: NULL. So Butter → `per_each` (the pound of a 36/1 LB case), Duke's → `per_each` (the gallon), Onion Powder ($6.65 vs $33.25 / 5 lb) → `per_lb`, Ovengold turkey → `per_lb`, Sour cream ($8.10 = $8.10 / 1/5 LB) → `per_case`. The migration column is nullable with a CHECK on the five values, no default. **Costing is untouched**: `computeSkuCostPerOz` keeps reading the ledger as it does; the basis is metadata for ordering and for the importer. |
| `item_number` (IN-*) | 68 (4 flagged) | **Attach where the sheet's evidence is a printed number on a receipt or an export row for the same vendor and an unambiguous description; otherwise leave NULL.** Rulings on the flagged four: IN-008 Baldor "Lettuce" → **ICE1** (iceberg cello 24 ct; the shop's lettuce is iceberg; the catalog's 15 × 15 oz pack is wrong and gets the pack rule above only if Juan later confirms — the number attaches, the pack does not). IN-021 Pickle slices → NULL (795 is whole pickles). IN-045 Utz Ripples → see vendor ruling below; attach **27149** once the SKU sits under Thompson Delivers. IN-068 plain "Eggs" → **517842** (medium), note "tentative — confirm at the door" (Juan: "I think medium"). |
| `vendor_merge` (VM-*) | 29 | **Boar's Head survives; Delmar Provisions (0 SKUs) → `active=false`**, notes "purveyor: Delmar Deli Provisions, Gaithersburg MD, BHDelmar@aol.com, 301-740-3956, Net 7" written to `vendors.notes` on the Boar's Head row if the column exists (append to existing notes, never overwrite). One audit row. |
| **NEW: `vendor_create` Thompson Delivers** | — | Juan 2026-09-18: Thompson Delivers (authorized Utz distributor, DSD, cash on delivery, cust C001190454) supplies the **12.5 oz Utz Ripples (9/box, $4.37/bag) and the 1 oz Mini Chips (60/box, $0.39/bag)**; Country Snacks supplies only the single-serve mixed-flavor bags (14/case, $1.80/bag). The seed creates vendor `Thompson Delivers` (active), re-points the Ripples 12.5 oz SKU and the Mini Chips SKU to it (`vendor_items.vendor_id`), attaches item numbers 27149 (Ripples) and leaves Mini Chips' number NULL (not on the ticket), appends prices: Ripples $4.37 `per_each` (bag) with a `box 9 × bag` level; Mini Chips $0.39 `per_each` with `box 60 × bag`; Country Snacks singles $1.80 `per_each` with `case 14 × bag`. If a "Utz Ripples" SKU today carries Country Snacks as vendor, it moves; if the catalog has ONE SKU standing for both the 12.5 oz and the single bag, the seed refuses in dry-run and lists it (`sku_identity_split_needed`) — I split it by hand before execute. |
| `guide_absent` (GA-*) | 41 | **No seed action.** Review list only; carried into the V3-C-2 importer's first dry-run as the human queue. |

## For Juan (informational, not blocking)
1. Eggs medium (517842) is attached as tentative; say "large" and one row flips.
2. Thompson Delivers becomes a real vendor with the two Utz SKUs under it; Country Snacks keeps the single bags.

## Seed shape (the law, restated for the author)
Dry-run first (prints every proposed write with its evidence line and the counts per class; refuses on any manifest mismatch against literal counts); execute writes in this order: migration 0210 (already applied by CC on the target), vendors (create Thompson, deactivate Delmar, notes), `vendor_items` (`vendor_id` re-points, `item_number` attaches, `price_basis`), `sku_pack_levels` (append new active level + retire old, only for exact item-number matches), `vendor_price_history` (append superseding rows). ONE audit row per class (`vendor.create`, `vendor.deactivate`, `vendor.merge`, `sku.item_number_set`, `sku.price_basis_set`, `sku.pack_level_supersede`, `sku.price_supersede`), `actor_context: "seed_38"`, `destructive` per the registry (deactivate/merge/supersede = true; set/create = false), registered in `lib/audit-actions.ts` in the same PR. Idempotent: a re-run finds every target already in its end state and writes nothing (`already` counts). Acceptance: `scripts/vendor-exports/diff.ts` against a fresh export after execute shows every priced SKU with a basis or in the `basis_unresolved` list, zero same-date conflicts among the LATEST rows, and the item-number join in section I rising from 51 present toward ≥ 100.
