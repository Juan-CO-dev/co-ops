/**
 * Location scoping engine — Phase 3.
 *
 * Functions:
 *   - getUserLocations(userId): UUID[]
 *   - canAccessLocation(user, locationId): boolean (level 7+ → all)
 *   - shouldShowLocationSelector(user): boolean (multi-loc only)
 *   - canViewAllLocations(user): boolean (level 7+)
 */
export {};
