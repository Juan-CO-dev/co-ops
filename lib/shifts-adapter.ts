/**
 * 7shifts adapter — scaffolded, deferred per spec Section 2.8.
 *
 * isEnabled(): boolean — reads SEVENSHIFTS_ENABLED env var
 * pullDailyData(locationId, businessDate): fetches and writes shifts_daily_data
 */
export {};
