/**
 * Toast POS adapter — scaffolded, deferred per spec Section 2.8.
 *
 * isEnabled(): boolean — reads TOAST_ENABLED env var
 * pullDailyData(locationId, businessDate): fetches and writes toast_daily_data
 * Activation = Phase 2 of the post-foundation roadmap.
 */
export {};
